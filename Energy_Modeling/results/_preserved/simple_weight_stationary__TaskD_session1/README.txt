TASK D SESSION 1's PLACEMENT FIGURE, copied aside by SESSION 2 before it
redrew anything.  2026-09-14.

  ReconSweep_optimiser__resnet18.png .csv .json
      simple_weight_stationary, resnet18, conv1 + layer3.0.conv1, BCH(63,39),
      ECC_SCOPE=dev, four metric rows (energy edp latency area).  This is
      session 1's own figure, committed in progress.txt at 0a05c4b and drawn
      with the OLD bar geometry and the ONE-colour saving annotation -- so it
      is also the BEFORE half of session 2's two figure-defect fixes.

  ReconSweep_optimiser__resnet18.pdf
      OLDER than the .png beside it (2026-09-13, eyeriss_like_wglb at full
      scope): session 1's run wrote png/csv/json and this pdf was left from
      the previous draw.  Not a matching pair.  Said here rather than quietly
      copied, because the stem carries no architecture and that is exactly the
      collision this directory exists for.

The stem `ReconSweep_optimiser__<model>` carries NO architecture, so every
placement run of every design overwrites this one path.
