# AGENTS.md

The project instructions live in **`CLAUDE.md`** at this same directory. Read
that file; this one is a pointer so agents that look for `AGENTS.md` find it.

Do not duplicate content here — a previous copy of `CLAUDE.md` lived at this
path, drifted out of date, and started contradicting it.
