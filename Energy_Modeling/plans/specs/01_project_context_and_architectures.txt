PROJECT CONTEXT: ECC WEIGHT RECONSTRUCTION IN AI ACCELERATOR DATAPATHS
PART 1: PROJECT FOUNDATIONS AND ARCHITECTURES — SECTIONS 1–11

Source conversation:
https://chatgpt.com/share/6a9cf72e-979c-83e9-932b-c6193b111007

Revised 2026-09-09: the ECC decoder moves onto the DRAM die and off the
fetch path, so the reduced representation starts at the DRAM interface
instead of at an on-chip ECC engine. Every diagram and table below is
written against that model. Section 1 defines it; nothing else is new.

PURPOSE OF THIS FILE

This file provides project context for an LLM coding assistant implementing
an architectural design-space exploration of ECC-derived weight
reconstruction in AI accelerators.

The primary target is Eyeriss v2. The methodology should also support:
- Eyeriss v1.
- Generic weight-stationary (WS) architectures.
- Generic output-stationary (OS) architectures.
- Generic input-stationary (IS) architectures.
- Simba.

The central idea is to let only the independent (message) portion of an
embedded-ECC weight representation leave the DRAM die, carry that reduced
form through selected parts of the on-chip hierarchy, and reconstruct the
omitted dependent bits with XOR encoders before computation.

Example:
- Original embedded weight representation: 8 bits.
- Retained information: approximately 3–4 bits per weight.
- Remaining bits: deterministically reconstructed from ECC relationships.

IMPORTANT:
The per-weight example is illustrative. Actual ECC dependencies span
multiple weights in a codeword. Part 2, section 15 explains this constraint.
Do not implement an independent per-weight encoder unless the ECC mapping
actually supports it.

The research question is not simply where XOR gates can be inserted. It is
where the reduced representation should stop and the full representation
should resume to minimize total cost while meeting throughput, timing,
capacity, and reliability requirements.

Three central quantities:
1. How many bits can remain omitted up to a candidate boundary?
2. How much temporal reuse occurs after reconstruction?
3. How much spatial fanout occurs before or after reconstruction?

Working framing:
"Reconstruction-boundary optimization from the DRAM interface across the
accelerator memory/interconnect hierarchy."

STATUS OF THE MATERIAL

- Architecture descriptions summarize the source discussion.
- Proposed placements, ratings, and expected winners are hypotheses.
- No measured energy savings, area results, or timing results are established
  by this document.
- Verify exact architecture configurations against the selected papers,
  repository configurations, and hardware models before implementation.
- Generic WS, OS, and IS describe dataflows; they do not uniquely specify a
  physical architecture.
- The original chat included citation markers. Those opaque chat-local
  markers are omitted here; they are not usable bibliographic references.

Historical motivation from the source discussion:
The original Eyeriss-style 65-nm energy comparison placed a DRAM access at
roughly 200 times MAC energy, a large global-buffer access at approximately
6 times, an inter-PE/array transfer at approximately 2 times, and a small RF
access at approximately 1 time.

These are historical illustrative ratios, not parameters to reuse
uncritically for a new technology or implementation.


======================================================================
1. CORRECT CONCEPTUAL MODEL FOR THE ECC SCHEME
======================================================================

An oversimplified view is:

DRAM --> SRAM --> RF --> PE
          ^
       Encoder?

A decoder on the fetch path forces the complete n-bit codeword across the
DRAM interface on every fetch. The model used here puts the decoder ON THE
DRAM DIE and OFF the fetch path:

                          DRAM DIE  (reliability domain)
+----------------------------------------------------------------+
| DRAM array: complete embedded-ECC codeword (n bits)            |
|          |                                                     |
|          v                                                     |
| On-die ECC decode / correction                                 |
|   runs at write, on a scrub pass, or on a prior access --      |
|   NOT on the fetch path                                        |
|          |                                                     |
|          v                                                     |
| Fetch: drive only the k message bits off the die;              |
|        the n-k parity positions never leave it                 |
+----------+-----------------------------------------------------+
           |
           |  DRAM INTERFACE (I/O, link, controller-to-accelerator port)
           |  carries k of every n bits  <-- the first saving, shared by
           |                                 EVERY reconstruction boundary
           v
       Reduced representation on chip
       Example: 4 retained bits instead of 8 full bits
           |
           |    ON-CHIP RECONSTRUCTION DOMAIN
           |
           +-------------- R1  (chip ingress)
           v
       Global storage
           |
           +-------------- R2
           v
          NoC
           |
           +-------------- R3
           v
       Local weight storage
           |
           +-------------- R4
           v
      Weight register/latch
           |
           +-------------- R5
           v
          MAC

Research question:

How deep into the accelerator can the reduced representation be propagated
before the cost of repeated reconstruction becomes larger than the
additional storage and communication energy saved? The DRAM-interface
saving is common to every boundary; the boundaries differ on chip.

Reliability constraint:

The reconstructable bits must not be discarded before ECC correction. With
an on-die decoder that constraint is met on the die: the array keeps and
reads the complete codeword, correction happens before any fetch, and the
discard happens at the die's output.

Required conceptual sequence:

Array holds full codeword --> on-die correction (off the fetch path)
    --> fetch selects k message bits --> interface carries k bits
    --> accelerator reduced-representation datapath

What IS credited at the DRAM (settled 2026-09-10):
- The WHOLE DRAM weight term scales by k/n under every reconstruction
  boundary. The access is custom-designed to collect only the interleaved
  message bits of each codeword, so the array reads fewer bits too; there is
  no array/interface split to apply and none is applied.

What is NOT credited at the DRAM:
- Decoder energy. The decoder is the same BCH decoder relocated to the
  DRAM die; it runs once per corrected codeword and sits outside the
  placement comparison. Record that it is DRAM-process logic.
- E_background and E_refresh. Both are 0 and unmodelled, which understates
  the embedded and reconstruction arms.

Reference bars: the conventional baseline is side-band ECC, so its array
holds the parity beside the data -- but that parity is corrected ON THE DIE
and never crosses the datapath, so the baseline moves the SAME weight bits as
the embedded arm and issues no extra reads. It pays for the bigger, indexed
array through a higher per-bit price (70 pJ/bit against 40), not through
traffic. The embedded arm without reconstruction consumes every weight bit on
chip, so its complete codeword crosses the interface.

Reconstructing at an SRAM input restores full-width data before storage,
so that SRAM does not receive the reduced-storage benefit. Upstream
transport, starting with the DRAM interface, still benefits.


======================================================================
2. MOST IMPORTANT PLACEMENT PRINCIPLE
======================================================================

Illustrative assumptions:
- Full weight representation: 8 bits.
- Reduced representation: 4 bits, born at the DRAM output pins.
- Reconstruction: several XOR operations.

2.1 EARLY RECONSTRUCTION

DRAM (on-die ECC)
   |
   | 4 bits
   v
Encoder at chip ingress
   |
   | 8 bits
   v
8-bit SRAM --8 bits--> NoC --8 bits--> RF --8 bits--> MAC

Benefits:
- Reduced DRAM-interface traffic.

Limitations:
- No reduced-width SRAM storage or traffic.
- No reduced-width NoC traffic after the encoder.
- No reduced-width RF storage or traffic.


2.2 LATE RECONSTRUCTION

DRAM (on-die ECC)
   |
   | 4 bits
   v
4-bit SRAM
   |
   +--4 bits--> NoC --4 bits--> 4-bit RF
                                  |
                                  v
                                Encoder
                                  |
                                  | 8 bits
                                  v
                                 MAC

Potential benefits:
- Reduced DRAM-interface traffic.
- Reduced SRAM storage and traffic.
- Reduced NoC traffic.
- Reduced RF storage.
- Reduced RF read bit-volume.

Main risk:
The encoder may execute once for every MAC use of the weight.


2.3 A RECONSTRUCTED-WEIGHT LATCH -- REJECTED, REMOVED FROM THE STUDY

Holding reconstructed weights in a latch between the weight SPad and the MAC,
so one reconstruction serves many MAC uses, was rated 5/5 and evaluated as
boundary R4b. It was measured and it does not work: consecutive weight reuse is
1 on 20 of the 21 resnet18 layers, so a latch catches nothing, and a register
that would pay has to hold the whole inner tile (16-384 weights) -- a second
scratchpad, not a latch. No dataflow in this study escapes that.

REMOVED 2026-09-10: no boundary on any design carries a per-PE reuse register,
and `eccenergy/recon.py` no longer implements one. Do not reintroduce one
without first measuring consecutive reuse on the target mapping. The
conclusion stands: reconstruct at the PE weight-SPad INPUT.


======================================================================
3. RATING SYSTEM
======================================================================

The source discussion used architectural promise ratings, not experimental
results.

| Rating | Interpretation |
|--------|----------------|
| 5/5 | Extremely promising reconstruction boundary |
| 4/5 | Strong candidate worth evaluating |
| 3/5 | Useful tradeoff point |
| 2/5 | Usually less attractive than another placement |
| 1/5 | Generally not worthwhile |

All ratings assume the reduced representation is physically exploited:
- Packed weights.
- Fewer accesses.
- Narrower transfers.
- Appropriate memory organization.

The DRAM-interface saving is the same for every boundary of one
architecture, so it does not change the relative ratings. It changes the
absolute saving of every boundary, including the earliest.

Merely leaving unused bits in a full-width word does not establish
proportional energy savings.


======================================================================
4. ENERGY MODEL
======================================================================

For reconstruction placement r, define:

n:
    Full representation width.

k:
    Retained representation width, where k < n.

A_l:
    Number of accesses/transfers at hierarchy level l.

E_l(b):
    Energy of accessing/transferring b bits at hierarchy level l.

N_rec(r):
    Number of reconstructions at placement r.

E_rec:
    Energy per reconstruction.

E_control(r):
    Additional control energy associated with placement r.

DRAM term. SETTLED 2026-09-10. The array/interface split (f_if) that this
section used to carry is REMOVED. The DRAM access in this architecture is
custom-designed to collect only the interleaved message bits of each codeword,
so the array reads fewer bits too and the WHOLE DRAM weight term scales:

E_DRAM(embedded, no reconstruction) = E_DRAM
E_DRAM(r), every boundary           = E_DRAM * k/n

There is no array/interface caveat to state and no f_if to cite. What remains
cited is the per-bit cost itself: 40 pJ/bit (FReaC Cache MICRO 2020; Gebhart
et al. MICRO 2012, 28-45 band), against the modeling tool's own 8 pJ/bit.

The conventional-ECC BASELINE is priced differently and it is NOT a traffic
term. Decoding happens on the DRAM die, so its parity is read, corrected and
discarded there and never crosses the datapath: the baseline issues the SAME
weight reads as the embedded arm. What it pays for is an array that also holds
the parity (2.625x the payload bits at BCH(63,30)) plus the indexing the other
two arms do not do, priced as 70 pJ/bit against 40 -- x1.75 on the whole DRAM
category of that arm only. Reconstruction is the only arm whose TRAFFIC scales.

E_background and E_refresh grow with the bigger array too. Both are currently
0 and unmodelled, which understates the embedded and reconstruction arms.

Reconstruction term. The synthesised engine has TWO energies with DIFFERENT
denominators, and adding them is dimensionally wrong:

E_rec_total = incremental_per_codeword * N_rec(r)
            + idle_per_cycle * total_cycles * N_engines(r)

incremental  per actual reconstruction -- an EVENT cost, so it can be put in
             front of the mapper (see 02_..., Sec. 19)
idle         per CYCLE the engine is powered, for every copy of it the
             placement needs. N_engines is 1 above the network and one per PE
             inside the array (168 on Eyeriss v1), so the placement choice
             moves this term by two orders of magnitude. It is a standby cost
             and no mapping can remove it -- only a shorter run can.

Conceptual model:

E(r) =
    E_DRAM(r)
  + SUM over l in partial(r) [A_l * E_l(k)]
  + SUM over l in full(r)    [A_l * E_l(n)]
  + E_rec_total(r)
  + E_control(r)

Boundary interpretation:

DRAM interface | Reduced representation | Full representation
               |                        ^
        always reduced             Reconstruction

Moving the boundary toward computation can save more data movement, but
may increase N_rec.

Implementation interpretation:
This is a conceptual accounting model. If packing or remapping changes
physical access counts, use placement-dependent physical counts rather
than assuming A_l remains identical for every variant.

The complete evaluation must also account for relevant leakage, buffering,
packing/unpacking, and timing overheads.

Use a consistent granularity for n, k, and E_rec:
- Per weight, only if reconstruction is independent per weight.
- Per ECC group/codeword, if dependencies span multiple weights.


======================================================================
5. EYERISS V2 — PRIMARY TARGET
======================================================================

5.1 ACTUAL HIERARCHY

Eyeriss v2 uses:
- Row-stationary-plus (RS+) dataflow.
- A hierarchical-mesh network on chip (HM-NoC).
- PE clusters.
- Router clusters.
- Global-buffer clusters.
- Separate networks for different data types.
- Communication modes supporting different reuse patterns.

The source discussion describes published PE-local CSC weight storage as:
- Weight-data scratchpad: 96 x 24 bits.
- Weight-address scratchpad: 16 x 7 bits.
- Capacity corresponding to up to 192 12-bit compressed weight entries.

These implementation-specific quantities should be verified before
hard-coding them into the model.

Important distinction:
The original Eyeriss-v2 GLB should not automatically be modeled as a
conventional large global weight SRAM. The source discussion states that
its GLB SRAM banks serve input activations and partial sums, with dedicated
weight ports feeding the weight network.

Weight-path abstraction:

DRAM array (complete embedded codeword; on-die ECC, off the fetch path)
  |
  v
DRAM interface: k message bits per codeword
  |
  v
Weight input port
  |
  v
Hierarchical weight NoC
  |
  +--> Mesh between clusters
  |
  v
Router cluster
  |
  v
Local PE-cluster distribution
  |
  v
PE weight address/data SPad
  |
  v
Sparse weight decode / scheduling
  |
  v
MAC datapath

Do not substitute this unconditionally:

DRAM --> Global weight SRAM --> RF --> MAC

The actual weight-residency and bypass behavior must match the architecture
being modeled.


5.2 RECONSTRUCTION FLOW

Candidate placements are alternatives, not a chain of encoders that all
operate simultaneously.

 DRAM array: complete embedded-ECC codeword
        |
        v
 On-die ECC correction  (off the fetch path)
        |
        v
 Fetch selects message bits; parity stays on the die
        |
        | DRAM interface: k of n bits   <-- saved under every boundary
        v
 Weight input port
        |
        +---- R1: reconstruct at chip ingress            [2/5]
        |
        v
 Weight HM-NoC ingress
        |
        | Reduced representation if not reconstructed
        v
 Inter-cluster mesh
        |
        +---- R2: reconstruct after global mesh          [4/5]
        |
        v
 Cluster-local router / fanout
        |
        v
 PE weight-SPad input
        |
        +---- R3: reconstruct before SPad storage        [4/5]
        |
        v
 Reduced weight SPad, if reconstruction is deferred
        |
        +---- R4a: reconstruct on each SPad read          [3/5]
        |
        v
      Encoder
        |
        v
       MAC

Why R1 is no longer a zero-saving control:
its encoder sits at the weight input port, so the interface already carried
k bits. It isolates the DRAM-interface saving from every on-chip saving.

Why R2 matters:

Long-distance cluster network
              |
              v
       Cluster-local network
              |
              v
             PEs

A cluster-boundary encoder can preserve reduced-width traffic through the
long-distance network and reconstruct once near a destination cluster:

4-bit representation
-------- Long HM-NoC -------->
                              Encoder
                                 |
                                 v
                               8 bits
                                 |
                                 v
                         Short local fanout

This is a compromise between:
- One centralized encoder.
- Many PE-local encoders.


5.3 EYERISS V2 PLACEMENT TABLE

| Hardware level | Function | Representation | Reconstruction option | Boundary | Advantages | Disadvantages | Rating |
|----------------|----------|----------------|-----------------------|----------|------------|---------------|--------|
| DRAM array | Stores protected embedded-ECC weights; on-die decoder corrects them off the fetch path | Full codeword | No | N/A | Retains off-chip protection; array unchanged | Complete codeword stored and read for correction; no array saving claimed | N/A |
| DRAM interface (I/O, link, controller port) | Drives only the k message bits of each corrected codeword to the chip | Reduced | Strip on the die, not reconstruct | DRAM output pins | Interface energy x k/n under EVERY boundary; the earliest saving | On-die decoder logic; message-bit selection within a burst | Essential |
| Weight input port / NoC source | Injects weights | Reduced before encoder | R1 | Chip ingress | Keeps the interface saving; one or a few encoders; simple | Expands traffic before the NoC; nothing on chip reduced | 2/5 |
| Inter-cluster mesh | Routes between clusters | Reduced preferred | R2 | Mesh output | Saves long-distance NoC traffic; fewer encoders than PE-local placement | Local network carries full weights | 4/5 |
| Cluster router/local distribution | Multicast/unicast to PEs | Reduced preferred | Cluster-local boundary | Output | Keeps much of HM-NoC reduced; localizes reconstruction | Replication and throughput requirements depend on distribution | 4/5 |
| PE weight-SPad input | Fills local weight memory | Reduced to full | R3 | SPad input | Reduced NoC traffic; reconstruction once per local fill | SPad remains full-width | 4/5 |
| PE weight SPad | Stores CSC weight data | Reduced | R4a | SPad output | Saves local capacity and access bit-volume | May repeatedly reconstruct the same weight | 3/5 |
| MAC input | Consumes full weight | Full | Very late placement possible | MAC input | Longest reduced-representation lifetime | Timing and throughput risk | 2/5 |


5.4 INITIAL EYERISS V2 VARIANTS

Every variant starts the same way:

DRAM array (on-die ECC) --k bits--> DRAM interface --k bits--> chip

EV2-A: RECONSTRUCT AT PE WEIGHT-SPAD INPUT

DRAM (on-die ECC)
   |
   v
4 bits ---- HM-NoC ----> Encoder --8 bits--> Weight SPad --> MAC

Expected savings:
- DRAM-interface traffic.
- Memory-interface-to-PE traffic.
- Hierarchical-mesh bandwidth.
- NoC switching.

Not reduced by this placement:
- PE weight-SPad capacity.
- PE weight-SPad read width.

Reconstruction frequency:
Approximately once per weight loaded into the local SPad, subject to actual
ECC-group dependencies and transfers.


EV2-B: RECONSTRUCT AT WEIGHT-SPAD OUTPUT

DRAM (on-die ECC)
   |
   v
4 bits ---- HM-NoC ----> 4-bit Weight SPad --> Encoder --8 bits--> MAC

Additional potential savings:
- PE weight-memory capacity.
- PE weight-memory write bit-volume.
- PE weight-memory read bit-volume.

Risk:
Reconstruction count can increase substantially.


EV2-C: WEIGHT-SPAD OUTPUT PLUS DECODED-WEIGHT LATCH
       -- REJECTED AND REMOVED, 2026-09-10 (see Sec. 2.3) --

4-bit SPad --> XOR encoder --> 8-bit decoded weight register --> MAC
                  ^                         |
                  |                         +--> Reuse across MAC uses
                  |
           Reload/reconstruct when the required weight changes

Leading hypothesis from the source discussion: 5/5.
MEASURED OUTCOME: rejected. "Reload when the required weight changes" happens on
every access -- consecutive weight reuse is 1 on 20 of 21 resnet18 layers -- and
a register that covers the whole cycled set has to hold up to 384 weights, the
size of the scratchpad it sits behind. Not implemented. Reconstruct at the SPad
INPUT (EV2-B / R3) instead.

Potential benefits:
- Reduced DRAM-interface traffic.
- Reduced HM-NoC traffic.
- Reduced SPad storage.
- Reduced SPad bit traffic.
- Amortized reconstruction.

The full-width register may be small compared with the SPad, but its actual
benefit depends on access order and reuse locality.


======================================================================
6. WEIGHT-STATIONARY
======================================================================

A weight-stationary dataflow keeps weights in PE-local storage while
activations stream and partial sums are updated or transferred according
to the implementation.

Canonical hierarchy:

DRAM array (on-die ECC)
  |
  v
DRAM interface / memory controller: k bits per codeword
  |
  v
Global/weight buffer
  |
  v
Weight-distribution NoC
  |
  v
PE weight RF
  |
  v
Stationary weight
  |
  v
MAC <--- Streaming activations


6.1 RECONSTRUCTION LOCATIONS

 DRAM array: complete codeword, corrected on the die
        |
        | DRAM interface: k of n bits   <-- saved under every boundary
        v
   Reduced weights
        |
        v
 Global weight SRAM
        |
        +--> R1: SRAM output                              [3/5]
        v
    Weight NoC
        |
        +--> R2: NoC output                               [4/5]
        v
 Reduced weight RF
        |
        +--> R3a: reconstruct on every read               [2/5]
        |
        v
      Encoder
        |
        v
 Full stationary-weight latch
        |
        +--> R3b: reconstruct once, reuse full weight
        |    -- REMOVED 2026-09-10, see Sec. 2.3 --
        v
       MAC

Illustrative example: one weight participates in 100 MACs.

Reconstruction before RF:
- 1 reconstruction.
- 100 full-width RF reads, under this illustrative access model.

Reduced RF plus reconstruction on every read:
- 100 reduced-width RF reads.
- 100 reconstructions.

Reduced RF plus stationary decoded register:
- 1 reduced RF read.
- 1 reconstruction.
- 100 register reuses.

These counts describe alternative access organizations, not a universal
property of all WS hardware. A fair baseline should include any stationary
register and reuse mechanism already present.


6.2 WEIGHT-STATIONARY TABLE

| Hardware level | Function | Weight representation/behavior | Reconstruction | Boundary | Advantages | Disadvantages | Rating |
|----------------|----------|--------------------------------|----------------|----------|------------|---------------|--------|
| DRAM array | Stores model weights; on-die decoder corrects off the fetch path | Full ECC | No | N/A | Off-chip reliability | Complete codeword read for correction; no array saving | N/A |
| DRAM interface / controller | Carries only the k message bits per corrected codeword | Full to reduced | Strip on the die | DRAM output pins | Interface energy x k/n under every boundary | On-die decoder logic | Essential |
| Global/weight buffer | Holds weight tiles | Reduced preferred | R1 | Buffer output | Global storage/access savings | Full-width downstream NoC | 3/5 |
| Weight NoC | Broadcasts/multicasts weights | Reduced preferred | R2 | NoC output/PE input | Reduced network traffic | Destination encoder replication | 4/5 |
| PE weight RF | Holds local weights | Reduced preferred | R3a | RF output | Reduced RF storage/read width | Repeated reconstruction without retained full weights | 2/5 |
| Decoded stationary register | Holds active full weight | Full | R3b | Register input | One reconstruction supports many uses | Small full-width storage/control cost | REMOVED 2026-09-10 (Sec. 2.3): "many uses" is 1 use on 20 of 21 layers |
| MAC | Repeatedly consumes weight | Full | Avoid repeated critical-path reconstruction | Input | Reuse opportunity | Timing/throughput risk | 2/5 |

Preferred hypothesis:

Reduced DRAM interface
      |
      v
Reduced global buffer
      |
      v
Reduced NoC
      |
      v
Reduced PE weight memory
      |
      v
XOR reconstruction
      |
      v
Small full-width stationary register
      |
      v
Repeated reuse
      |
      v
MAC


======================================================================
7. EYERISS V1
======================================================================

The source discussion describes original Eyeriss as:
- Row-stationary (RS) dataflow.
- 168-PE spatial array.
- 108-kB global buffer.
- Approximately 0.5 kB of local RF per PE in the fabricated chip.

Important modeling distinction -- SETTLED 2026-09-10:
The referenced official Timeloop Eyeriss example contains:
- ifmap_spad
- weights_spad
- psum_spad

and it explicitly bypasses weights at shared_glb. That example is NOT the
published chip. JSSC 2017 Sec. V-A allocates 8 kB of the 108 kB GLB (two banks
of 512-b x 64-b SRAMs) to filter weights: "Even though it is not required by
the dataflow ... the GLB preloads the filters used by the next processing
pass." The file that models that allocation IS Eyeriss v1 for this study --
`archs/eyeriss_like_wglb/arch_paper.yaml`. `archs/eyeriss_like/arch_paper.yaml`,
which declares `!Nothing` where the filter GLB sits, is RETIRED; the
bracketing-pair doctrine for v1 is over.

The consequence for this section is one extra reconstruction boundary. A
weight level ABOVE the PE array admits a boundary at its output that a design
with no weight GLB has nowhere to put, and FINDINGS 7.8 is why it matters:
refetch on v1 is set by the DRAM-level loop order over P and Q, a weight tile
cannot index either, so a weight buffer INSIDE the array cannot absorb those
loops however large it is made (measured flat to x32 capacity at 1 % fill).
`filter_glb` sits above the array and can.

Useful abstraction:

DRAM array (on-die ECC)
  |
  v
DRAM interface / iFIFO: k bits per codeword
  |
  v
Filter GLB: 8 kB of the published 108 kB, weights only
  |
  v
Flat array NoC: 14-way multicast across PE columns
  |
  v
Column-local distribution to the 12 PEs of a column
  |
  v
PE pFIFO
  |
  v
PE weight SPad/RF
  |
  v
MAC

The flat array network is TWO stages as modelled, not one -- Timeloop's
PE_column fanout (meshX 14) and its PE fanout (meshY 12) -- and each admits its
own boundary. The PE pFIFO has no level in the model, so Sec. 7.2's
FIFO-output boundary does not exist to evaluate and is absent by construction
rather than omitted.

RS seeks reuse of weights, activations, and partial sums across RF and
array levels instead of optimizing only one tensor.


7.1 RECONSTRUCTION DIAGRAM

FIVE boundaries since the filter GLB was adopted on 2026-09-10, not three.
The numbering follows weight-stationary's (Sec. 6.2) -- the other design in the
study with a weight buffer above its network -- so R2 is the global weight
buffer's output on both. `eccenergy/recon.py` `_EYERISS_V1_WGLB_PLACEMENTS` is
this list, key for key.

DRAM array: complete codeword, corrected on the die
  |
  | DRAM interface: k of n bits   <-- saved under every boundary
  v
Reduced weights at chip ingress
  |
  +--> R1: source-side reconstruction, before the GLB      [3/5]
  |         nothing on chip carries the reduced form; the
  |         control that isolates the interface saving
  v
Filter GLB (8 kB, weights only)
  |
  +--> R2: reconstruct at the filter-GLB output            [3/5]
  |         the GLB holds the reduced form; encoder at its
  |         read port, before the array network
  v
Flat array NoC: 14-way multicast across PE columns
  |
  +--> R3: reconstruct at the column edge                  [4/5]
  |         one encoder per COLUMN, so the count is
  |         arrivals = ingresses x multicast factor
  v
Column-local distribution to the 12 PEs
  |
  +--> R4: reconstruct at the PE filter-spad input         [4/5]
  |         encoder at the scratchpad WRITE port; runs once
  |         per weight FILLED into a PE -- the smallest
  |         count of any boundary below the network
  v
PE pFIFO  (no level in the model; Sec. 7.2's FIFO boundary
  |        does not exist to evaluate)
  v
Reduced weight SPad/RF
  |
  +--> R5a: reconstruct on every filter-spad read          [3/5]
  |          encoder at the READ port; the MAC reads the
  |          spad directly, so it runs once per MAC
  v
Encoder
  |
  v
MAC

Which boundaries the MAPPER can move, and which it cannot (2026-09-10):
R5a's count is the spad read count, which is the MAC count, so it is fixed by
the layer and no mapping can change it. R1's count is the DRAM weight read
count, which tiling moves only weakly. R2's (GLB reads) and R4's (spad fills)
are set by the tiling and the spatial mapping and move by multiples, so those
two are the ones worth putting in front of the mapper. See prompt_6.md.

Multicast tradeoff:

BEFORE MULTICAST

4 bits
  |
  v
Encoder
  |
  v
8 bits --------------+--> PE
                     +--> PE
                     +--> PE
                     +--> PE

One reconstruction at the source, but full-width network traffic.


AFTER MULTICAST

4 bits --------------+--> Encoder --> PE
                     +--> Encoder --> PE
                     +--> Encoder --> PE
                     +--> Encoder --> PE

Replicated reconstruction hardware, but reduced-width shared transport.

Make multicast fanout versus reconstruction multiplicity an experiment
variable.


7.2 EYERISS V1 TABLE

| Level | Function | Reconstruction option | Boundary | Main saving | Main cost | Rating |
|-------|----------|-----------------------|----------|-------------|-----------|--------|
| DRAM array | Protected weight storage; on-die correction off the fetch path | No | N/A | N/A | Complete codeword read for correction | N/A |
| DRAM interface / iFIFO | Carries k message bits per codeword | Strip on the die | DRAM output pins | Interface energy x k/n, every boundary | On-die decoder logic | Essential |
| Chip ingress | Receives the reduced stream | R1 source reconstruction | Before the GLB | Interface only; the control | Full-width GLB, network and spad | 3/5 |
| Filter GLB (8 kB of 108 kB) | Preloads the next pass's filters | R2 GLB-output reconstruction | Output | GLB capacity and per-weight read cost | GLB is read once per weight DELIVERED into the array, not once per weight stored | 3/5 |
| Flat array NoC (14-way column multicast) | Multicasts weights across PE columns | R3 destination reconstruction | Column edge | Network-width saving on the long hop | One encoder per column; count = ingresses x multicast factor (up to 7x) | 4/5 |
| Column-local distribution (12 PEs) | Distributes within a column | -- folded into R4 -- | -- | -- | -- | -- |
| PE FIFO | Buffers incoming weights | FIFO-output reconstruction | Output | Keeps transport reduced | NO LEVEL IN THE MODEL -- absent by construction, not omitted | not evaluable |
| PE weight RF/SPad, write port | Receives weights into the PE | R4 spad-input reconstruction | Input | Whole network carries the reduced form | One encoder per weight FILLED into a PE | 4/5 |
| PE weight RF/SPad, read port | Exploits RS reuse | R5a local-output reconstruction | Output | Spad capacity, write AND read bit-volume | The MAC reads the spad directly, so the encoder runs once per MAC | 3/5 |
| MAC | Executes products | Very late reconstruction possible | Input | Maximum reduced lifetime | Timing risk | 2/5 |

Boundary keys, counts, and what the mapper can reach:

| Key | Boundary | Reconstruction count | Mapping-sensitive | ERT hook |
|-----|----------|----------------------|-------------------|----------|
| recon1 | R1 @ chip ingress | DRAM weight reads | weakly | none (post-processed) |
| recon2 | R2 @ filter-GLB output | filter_glb READS | strongly | filter_glb `read` action |
| recon3 | R3 @ column edge | array-NoC deliveries | strongly | none -- the count lives in the NoC model, not in a component action |
| recon4 | R4 @ PE spad input | weights_spad FILLS | strongly | weights_spad `write` action |
| recon5 | R5a @ every spad read | weights_spad reads = MACs | NO -- fixed by the layer | pointless: a constant added to every mapping cannot change the argmax |

Only recon2 and recon4 have both a mapping-sensitive count and a storage
action to attach an encoder to, so those are the two the mapping optimiser is
given. The rest are post-processed on the reference mapping. prompt_6.md.


======================================================================
8. OUTPUT-STATIONARY
======================================================================

In output-stationary dataflow, partial sums remain local while weights and
activations are supplied to the PE.

Weights ----> PE: partial sum remains here
Inputs  ----> PE

Canonical hierarchy:

DRAM array (on-die ECC)
  |
  v
DRAM interface: k bits per codeword
  |
  v
Weight buffer / global SRAM
  |
  v
NoC / systolic injection
  |
  v
PE
  |
  +--> Streamed weight
  +--> Streamed activation
  +--> Stationary partial-sum RF
  |
  v
Output

Weights are streaming operands in this abstraction.

Reconstructing much earlier than the PE expands the weight representation
while it is still being transported.


8.1 RECONSTRUCTION DIAGRAM

DRAM array: complete codeword, corrected on the die
    |
    | DRAM interface: k of n bits   <-- saved under every boundary
    v
Reduced weights
    |
    v
Reduced global weight SRAM
    |
    +--> R1: global SRAM output             [3/5]
    v
Reduced weight NoC
    |
    +--> R2: destination-side NoC output    [5/5]
    v
PE input FIFO
    |
    +--> R3: FIFO output                    [4/5]
    v
Encoder
    |
    v
MAC
    |
    v
Stationary partial sum

Late reconstruction is promising when weights stream without substantial
local temporal reuse.

OS alone does not prove that weights have no reuse. Actual mapping,
multicast, forwarding, and buffering still determine the counts.


8.2 OUTPUT-STATIONARY TABLE

| Level | Function | Weight behavior | Reconstruction | Advantages | Disadvantages | Rating |
|-------|----------|-----------------|----------------|------------|---------------|--------|
| DRAM array | Full embedded-ECC storage; on-die correction off the fetch path | Stored | No | Complete protection | Complete codeword read; no array saving | N/A |
| DRAM interface / controller | Carries k message bits per codeword | Full to reduced | Strip on the die | Interface energy x k/n, every boundary | On-die decoder logic | Essential |
| Weight SRAM/GLB | Buffers weights | Reduced | Output | SRAM saving | Expands before NoC | 3/5 |
| Weight NoC | Delivers weights | Reduced | Destination | Reduced bandwidth/switching | Encoder duplication may be needed | 5/5 |
| PE input FIFO | Buffers incoming weights | Reduced | FIFO output | Narrow local FIFO | Reconstruction per delivered weight/group | 4/5 |
| MAC operand latch | Holds short-lived operand | Full | Latch input | Very late reconstruction | Must sustain operand demand | REMOVED 2026-09-10 (Sec. 2.3); the MAC-input boundary itself is retained and reported INFEASIBLE (G_rec = 9) |
| Partial-sum RF | Stores outputs | Not weight storage | No | N/A | N/A | N/A |

Promising path:

DRAM (on-die ECC) --4 bits--> NoC ----> PE --> XOR --> MAC

Earlier alternative:

DRAM (on-die ECC) --4 bits--> XOR --> 8-bit weight ---- NoC ----> PE

The earlier alternative keeps the interface saving but sacrifices
reduced-width NoC traffic.

High multicast fanout can change the result:

Option 1:
1 encoder + full-width multicast to 100 PEs.

Option 2:
100 destination encoders + reduced-width multicast.

Evaluate both rather than assuming late reconstruction always wins.


======================================================================
9. INPUT-STATIONARY
======================================================================

Input-stationary dataflow retains activations locally while weights are
supplied to the array.

             Stationary activation
                     |
                     v
Weights -----------> PE -----------> Partial sum
Weights -----------> PE -----------> Partial sum
Weights -----------> PE -----------> Partial sum

For weight reconstruction, IS resembles the streaming-weight OS
abstraction. The difference is which other operand receives stationary
reuse.


9.1 RECONSTRUCTION DIAGRAM

DRAM array: complete codeword, corrected on the die
    |
    | DRAM interface: k of n bits   <-- saved under every boundary
    v
Reduced weights
    |
    v
Reduced global weight buffer
    |
    +--> R1: global-buffer output          [3/5]
    v
Reduced NoC / systolic stream
    |
    +--> R2: destination-side NoC output   [5/5]
    v
PE weight input
    |
    +--> R3: local input-buffer output     [4/5]
    v
XOR reconstruction
    |
    v
MAC <--- Stationary activation RF

Many weights may be applied to the same stationary input, creating
substantial weight traffic. Reducing weight transport, from the DRAM
interface onward, can therefore be valuable.


9.2 INPUT-STATIONARY TABLE

| Level | Role | Weight behavior | Reconstruction | Benefit | Cost | Rating |
|-------|------|-----------------|----------------|---------|------|--------|
| DRAM array | Protected model storage; on-die correction off the fetch path | Stored | No | ECC protection | Complete codeword read; no array saving | N/A |
| DRAM interface / controller | Carries k message bits per codeword | Full to reduced | Strip on the die | Interface energy x k/n, every boundary | On-die decoder logic | Essential |
| Global weight buffer | Supplies changing weights | Reduced | Output | SRAM saving | Full downstream traffic | 3/5 |
| Weight NoC | Streams filters past activations | Reduced | NoC output | Transport/bandwidth reduction | Replicated encoders | 5/5 |
| PE weight FIFO/register | Temporary buffering | Reduced | Output | Local-buffer saving | Reconstruction for each new weight/group | 4/5 |
| MAC input | Supplies weight operand | Full | Input | Latest boundary | Must sustain required operand rate | 4/5 |
| Activation RF | Holds stationary input | Not weight storage | No | Outside weight-reconstruction path | N/A | N/A |

Leading hypothesis:
Destination-side NoC reconstruction is a strong IS candidate.


======================================================================
10. SIMBA
======================================================================

Simba provides a multi-level weight-stationary architecture.

Hierarchy:

Package
   |
   v
Chiplet
   |
   v
PE

Communication:
- Network on package (NoP) between chiplets.
- Network on chip (NoC) within a chiplet.

The source discussion describes each PE as containing:
- Distributed weight buffer.
- Input buffer.
- Parallel vector-MAC units.
- Accumulation buffer.
- Post-processing hardware.

Key feature:
Weights remain in vector-MAC registers and are reused while new inputs
are supplied each cycle.

This aligns with reconstruction immediately before stationary weight
registers.


10.1 FLOW

DRAM array (on-die ECC)
  |
  v
DRAM interface / memory controller: k bits per codeword
  |
  v
Global PE / global storage
  |
  v
NoP
  |
  v
Chiplet router
  |
  v
Chiplet NoC
  |
  v
PE router interface
  |
  v
Distributed weight buffer
  |
  v
Vector-MAC weight registers
  |
  v
Vector MACs

Treat this as an abstraction to reconcile with the chosen Simba model.
Do not invent a storage level merely because it appears in this diagram.


10.2 RECONSTRUCTION DIAGRAM

DRAM array: complete codeword, corrected on the die
   |
   | DRAM interface: k of n bits   <-- saved under every boundary
   v
Reduced representation
   |
   +--> R1: source/global reconstruction       [2/5]
   v
Global PE / package network
   |
   v
NoP
   |
   +--> R2: NoP output                         [3/5]
   v
Chiplet NoC
   |
   +--> R3: chiplet NoC output                 [4/5]
   v
PE distributed weight buffer
   |
   | Reduced representation
   |
   +--> R4: distributed weight-buffer output   [5/5]
   v
XOR reconstruction
   |
   v
Full weight
   |
   v
Vector-MAC stationary register
   |
   +--> reuse
   +--> reuse
   +--> reuse
   +--> reuse --> Vector MAC


10.3 SIMBA TABLE

| Level | Function | Representation | Reconstruction | Advantages | Disadvantages | Rating |
|-------|----------|----------------|----------------|------------|---------------|--------|
| DRAM array | Stores model; on-die decoder corrects off the fetch path | Full ECC | No | Full reliability | Complete codeword read; no array saving | N/A |
| DRAM interface / memory controller | Carries k message bits per codeword | Full to reduced | Strip on the die | Interface energy x k/n, every boundary | On-die decoder logic | Essential |
| Global PE/source storage | Supplies chiplet-level data | Reduced | R1 | Centralized encoder simplicity | Expands before NoP and NoC | 2/5 |
| NoP | Package/chiplet communication | Reduced | R2 at output | Saves package communication | Chiplet NoC carries full data | 3/5 |
| Chiplet NoC | Routes to PEs | Reduced | R3 at output | Saves NoP and NoC traffic | Full PE weight-buffer storage | 4/5 |
| Distributed weight buffer | Holds local weight tile | Reduced | R4 at output | Saves NoP, NoC, local storage/access | Local encoder needed | 5/5 |
| Vector MAC | Reuses stationary weight | Full | Avoid encoding every MAC | Strong amortization opportunity | N/A | N/A |

Promising implementation:

Reduced DRAM interface
              |
              v
Reduced distributed weight buffer
              |
              v
           One read
              |
              v
         XOR encoder
              |
              v
    Vector-MAC weight register
              |
      +-------+-------+
      v       v       v
     Use     Use     Use ...

Simba already has stationary vector-MAC weight registers. Determine whether
the proposed implementation can reuse existing registers rather than
assuming an additional register is necessary.


======================================================================
11. CROSS-ARCHITECTURE COMPARISON
======================================================================

These rankings are hypotheses. The DRAM saving is common to every column of
one row, so it moves every boundary of an architecture by the same amount and
does not reorder the columns.

A fifth column, "local output + decoded reuse register", was rated 5/5 on
every row until 2026-09-10. It is deleted, not struck through: it was measured
and it does not exist as a useful option (Sec. 2.3).

| Architecture | Early/global reconstruction | After NoC | Local weight-storage output | Likely best candidate |
|--------------|-----------------------------|-----------|-----------------------------|-----------------------|
| Eyeriss v2 | 2/5 | 4/5 | 3/5 | SPad INPUT (R3) |
| Weight stationary | 3/5 | 4/5 | 2/5 for naive repeated encoding | PE INPUT (R3) |
| Eyeriss v1 | 3/5 | 4/5 | 3/5 | SPad INPUT (R3) |
| Output stationary | 3/5 | 5/5 | 4/5 | Destination-side NoC/PE input |
| Input stationary | 3/5 | 5/5 | 4/5 | Destination-side NoC/PE input |
| Simba | 2/5 | 4/5 | 5/5 | Distributed weight-buffer output |

MEASURED OUTCOME (2026-09-10): the best feasible boundary is the PE weight-SPad
INPUT (R3) on both designs evaluated -- +11.07% vs embedded on
simple_weight_stationary and +17.24% on eyeriss_like. It needs no extra state
and no mapping constraint.

General hypothesis, as originally written and now FALSIFIED:

Architectures with strong local weight reuse favor reconstruction
immediately before a small stationary decoded-weight register.
  -> The premise fails: the mappings these designs actually choose walk weight
     dimensions in the innermost temporal loops, so there is no local weight
     reuse for a small register to exploit.

Architectures with streaming weights favor reconstruction after the
weight-distribution network, subject to fanout, forwarding, and encoder
throughput costs.

Architectures whose weight energy is dominated by DRAM refetch gain most
from the interface term, which every boundary receives.

Do not hard-code these rankings as the outcome of the DSE.
The implementation must measure the relevant counts and costs.

END OF PART 1

Continue with Part 2 for:
- Temporal reuse and spatial fanout.
- Break-even analysis.
- CSC interaction.
- ECC dependency granularity.
- Physical memory packing.
- Experiment variants and metrics.
- Timeloop/Accelergy and RTL synthesis integration.
- Initial experiments and research hypotheses.
