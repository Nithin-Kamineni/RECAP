PROGRESS CHECKLIST  --  ECC accelerator energy modelling
========================================================
Last updated : 2026-09-08 (R4b REUSE-REGISTER AUDIT, closed -- see FINDINGS.md section 7.1
               and recon.ReuseRegister. R4b was charging its register a write and nothing
               else while its whole reconstruction saving came from that register serving
               the scratchpad reads; the register now holds only the n-k regenerated bits,
               which is consistent, costs the PE nothing in storage, and moves the headline
               1.36% -> 1.30% at BCH(63,51). Previously 2026-09-07: Task 3 SECOND-PASS
               REVIEW -- is the K/N reduction applied, and is its magnitude right? -- see
               the TASK 3 REVIEW block below and FINDINGS.md section 15 "Review, second
               pass". Task 3 implemented + evaluated, Task 2 embedded ECC and the NoC
               model earlier the same day)

R4b REUSE-REGISTER AUDIT (2026-09-08)  -- read FINDINGS 7.1 before reopening R4b
---------------------------------------------------------------------------------
✅ Found: N_rec=fills for R4b assumed the register served 82.85 uses/reconstruction,
   while the SPad's 1,814,073,344 reads (97.6% of its 369.276 uJ) were still billed
✅ Fixed: ECC_RECON_REUSE_REG_MODEL. Default `complement` -- the register holds only
   the n-k bits the encoder regenerates, weight_bits*(1-K/N) per weight, so it cannot
   serve a read alone. SPad read count legitimately unchanged; PE weight storage
   1.00x the baseline at every K (was reported as 1.81x)
✅ The opposite reading is a RUNNABLE row, not prose: `full_width` serves the reads
   (+6.13%), and every result now carries what the same register would give a PE with
   NO ECC (+6.17%, ECC's own marginal +0.16%) -- which collapses to about zero once
   the register is priced at weights_spad's energy instead of ifmap_spad's, because a
   2048-bit register cannot beat the 2304-bit register file it sits in front of
✅ `free` reproduces the pre-2026-09-08 numbers exactly, so the diff is checkable
✅ 31 tests (was 26), none skipped; 11 mutations caught (was 6). The full_width
   read-removal mutation went UNCAUGHT until a test ran it against the real cache --
   the synthetic fixture has no ERT, so that path never executed
✅ Task 1/2 frozen code unchanged: baseline 5,657.062 uJ / embedded 4,798.375 uJ
   bit-identical across every run before and after
Legend       : ✅ done   ❌ not done / wrong   ⚠️ done with caveats or partially

How to read this: one line per task or subtask, each marked with exactly one
emoji. The plan being executed is 03_staged_implementation_plan.txt. Numbers
and reasoning live in FINDINGS.md; this file is only the status board.


SHARED DEVELOPMENT MODE (required from Task 1 on)
--------------------------------------------------
✅ Select one/two real layers by stable name (ECC_LAYERS), full model when empty
✅ Mapping generation separated from evaluation (`bash run.sh map` vs `baseline --eval`)
✅ Mapper cache keyed by arch YAML + mapper settings (fp-<hash> + mapping.json sidecar)
⚠️ Cache key STABILITY: the fingerprint recipe changed after Task 1 (search_size and
   max_permutations were added to mapper_settings), so the Task 1 mapper-cache entries
   (fp-25b35c9b0d5a etc.) are no longer addressable by the current code; the Task 1 result
   JSONs cannot be re-evaluated with `--eval`. Re-map at the current recipe instead.
✅ Raw-energy cache now rejects a record whose per-layer shape list differs from the
   current workload (added 2026-09-06; needed by the depthwise fix)
✅ Quick-run commands documented with bounded search and pinned thread count
⚠️ Fixed seed: Timeloop v4 exposes none; thread count is pinned instead (documented)
✅ Machine-readable results with units, config ids, mapping ids (results/evaluation/)


TASK 1  --  BASELINE ARCHITECTURES + EXTERNAL BCH PARITY
--------------------------------------------------------
✅ Architectures organised consistently (archs/<name>/arch_paper.yaml + archs/_shared/)
✅ Shared contract validated: `bash run.sh validate` -> 6/6 (7/7 with wglb) pass, 0 violations
✅ Precisions explicit and distinct (w8 / a8 / per-design accumulator 16/20/24 b)
✅ Hierarchy, capacity, bypass rules preserved per design (printed by validate)
✅ Conventional-ECC baseline implemented (external parity, whole-weight codewords,
   message + tail padding, DRAM-word granularity); hand check passes on every result
✅ External parity kept out of on-chip SRAM/RF (ECC_BASELINE_INFLATES_ONCHIP=0 default)
⚠️ "Each architecture completes the selected-layer quick run": TRUE mechanically, but
   the Task 1 quick-run NUMBERS are not usable -- see the mapper item below
❌ Baseline energies trustworthy enough to compare architectures: NO.
   Under the Task 1 search (hybrid, victory 100) Eyeriss v1/v2 cost ~5x more than under a
   wider random_pruned search on the same two layers (1979/2037 uJ -> 390/402 uJ), and the
   ranking inverts. The cached figures were produced by an even more bounded search.
⚠️ Existing baseline preserved apart from documented changes: the documented changes
   are real; the undocumented one is the fingerprint-recipe change above


WORKLOAD CORRECTNESS (audit items from the 2026-09-06 request)
---------------------------------------------------------------
✅ Depthwise / grouped convolutions: defect CONFIRMED and FIXED.
   Old model: C=1 input channel shared by all M filters (input tensor 960x too small).
   New model: group dimension G in the Timeloop problem (Layer.G, M per group), grouped
   problem template, shape names carry _G<g>. Verified: DRAM Inputs 81 -> 77,760 scalars.
✅ Grouped shape maps on all 7 architectures (smoke test, tiny search)
✅ Depthwise ENERGY at converged settings: DONE for mobilenet_v2 on all six architectures
   (31 distinct shapes, 10 depthwise), mapped on HiPerGator 2026-09-06 (see FINDINGS.md §13)
✅ ConvNeXt per-pixel nn.Linear layers (pwconv1/pwconv2) recorded as single-pixel
   matmuls: FIXED in generate.py, convnext_tiny regenerated (4.46 G MACs vs paper 4.5 G)
✅ MAC counts checked against published values: resnet18 1.81 G, resnet50 4.09 G,
   mobilenet_v2 300.8 M, efficientnet_b0 385.8 M, convnext_tiny 4.46 G
❌ Every other model with depthwise layers (efficientnet_b0, xception, convnext_tiny)
   still needs its mapper cache rebuilt for the new grouped shapes before any figure


MAPPER / EVALUATION CONSISTENCY
--------------------------------
❌ Search convergence: `hybrid` (the run.sh default) under-converges badly on the Eyeriss
   designs (196x-224x DRAM weight refetch on layer1.0.conv1 even at victory 500). The
   default must change or every ranking is a search artefact. Decision for the user.
✅ Evidence gathered: same layers, 4 search settings, all 7 designs (FINDINGS.md §2)
✅ The panel figure ArchitectureSweep__panels__resnet18__mobilenet_v2.png was REGENERATED
   2026-09-06 from whole-model mappings at the converged search with corrected grouped
   shapes, and is quotable. The old bounded-search (ss300/perm4) version is kept at
   results/figures/_superseded_bounded_search/ and must still not be quoted
⚠️ Two fingerprints exist per treatment for several designs (YAMLs were edited between
   runs); only the one the current YAML produces is valid -- always compute it, never glob
✅ Stale test (mapper_slug gained -ssnone-perm16) fixed; 15/15 offline tests pass
✅ Evaluation-only rerun path (`baseline --eval`) verified to need no container


EYERISS V2 MODEL CHECK
----------------------
✅ It is a DENSE Eyeriss v2: own hierarchy (16 clusters x 12 PEs x SIMD-2 = 384 MACs,
   72 kB iact GLB + 120 kB psum GLB, no weight GLB, 96x24b weight spad = 288 weights/PE,
   24 B iact spad, 32x20b psum spad, 20 b psums, RS+ spatial constraints). It is NOT the
   stock eyeriss_like renamed.
⚠️ Not modelled, by design or by tool limit: CSC sparsity (dense on purpose), HM-NoC
   router energy, NoC mode switching, 12 b register words (declared 24x8b), 65 nm (45 nm
   used), 200 MHz (1 GHz cycle used for leakage)
⚠️ Order-of-magnitude sanity vs the paper only (3.3 pJ/MAC here at 45 nm without NoC,
   vs ~11 pJ/MAC published at 65 nm with everything); no direct validation possible


ENERGY BREAKDOWN (component level, not totals)
-----------------------------------------------
✅ Per-(level, dataspace) breakdown available for every raw record (levels[] in
   results/_raw and detail.per_level in results/evaluation); script in FINDINGS.md §7
❌ NoC energy: ZERO for every architecture (Timeloop "Networks" section empty; no
   network components declared). Comparisons are missing the interconnect term.
✅ DRAM per-scalar cost identical across designs (64 pJ / 8-bit access) -- verified


MIGRATION TO HIPERGATOR (decided 2026-09-06: local runs take ~12 h; target is minutes)
-----------------------------------------------------------------------------------
✅ Instructions written: hpc/HIPERGATOR.md (transfer, image build, verification checklist,
   VS Code workflow, what is known about parallelisation) + hpc/tl.sh (apptainer wrapper);
   pointer section added to CLAUDE.md
✅ Project copied into /blue/rewetz/vkamineni/Projects/RECAP/Energy_Modeling; the nested
   duplicate was verified to hold 0 unique files and removed 2026-09-06 (freed 650 MB)
✅ LF guard run; required directories and system-python eccenergy import verified
✅ Copy integrity: all 23,249 mapper-cache files and 101 raw-cache files matched the source
✅ timeloop.sif built with Apptainer 1.4.2 in four-core allocation 41258861; cache/temp under /blue
✅ Five verification checks passed in 18-core allocation 41260181; six laptop totals match exactly
✅ CACTI writable scratch bind added to hpc/tl.sh; uncached shape mapped in 28.724 s with sidecar
✅ Account/QOS confirmed: rewetz; investment concurrent-core limit 181 (slurmInfo)
✅ Parallel (job-array) execution designed and built ON HiPerGator, after §5:
   hpc/map_array.sbatch + hpc/tasks_panel.txt (one (arch, model) per array task),
   hpc/eval_panel.sh (cache-only evaluation) and hpc/summary.py (the model x arch matrix).
   Array 41269118, --array=1-12%9, 18 cores per task, 162 cores held: 12/12 COMPLETED,
   0 failed, 258 shape-mappings in 48 min wall (longest task 30m49s)
✅ Two shared-file races fixed rather than avoided: patched_arch_path() and write_globals()
   now write through _write_atomic() (temp file + os.replace) in eccenergy/archs.py.
   Content is unchanged -- section 5 check 4 still reproduces the six laptop totals exactly
⚠️ Local chained full-model run (run_full_models.sh) stopped/abandoned 2026-09-06 15:15 with
   the resnet18 representative layers mapped for 4 of 7 designs; the cache is in the transfer

FULL-MODEL EVALUATION (converged search, 6 architectures)  -- superseded the layer subset
-----------------------------------------------------------------------------------------
✅ resnet18 WHOLE MODEL (21/21 layers, 12 distinct shapes) mapped and evaluated on all six
   architectures; every parity hand check PASS
✅ mobilenet_v2 WHOLE MODEL (53/53 layers, 31 distinct shapes, 10 of them depthwise) mapped
   and evaluated on all six architectures; every parity hand check PASS
✅ Panel figure regenerated from this data at converged settings with the corrected grouped
   shapes: results/figures/ArchitectureSweep__panels__resnet18__mobilenet_v2.{png,pdf}.
   The previous bounded-search version is kept at
   results/figures/_superseded_bounded_search/ and must still not be quoted
✅ Expected-ordering comparison: the architecture ranking is PRESERVED across the two models
   (v2 ~ v1 < Simba-like ~ input-stationary < weight-stationary < output-stationary), which is
   the claim the two-panel layout exists to test
⚠️ The two development layers are NOT a proxy for the model: on the layer pair the order was
   v1 < v2 < Simba < WS < IS < OS, but on the whole model WS and IS swap (WS is 20% WORSE than
   IS) and v1/v2 swap. Never rank architectures from the layer subset
⚠️ eyeriss_like DRAM weight refetch is x1.25 on the whole model at these settings, not the ~7x
   quoted in CLAUDE.md from the bounded-search era; that caveat needs re-measuring
❌ Remaining six models (resnet50, densenet121, squeezenet1_1, efficientnet_b0, convnext_tiny,
   xception) -- 221 distinct shapes x 6 archs = 1,326 tasks; not run, sized in HIPERGATOR.md §7
❌ eyeriss_like_wglb / eyeriss_v2_like_wglb bracketing pair NOT included in the sweep, so no
   Eyeriss v1 bound is available from this run (CLAUDE.md requires quoting the pair)


INTERCONNECT (NoC) ENERGY MODEL  --  2026-09-07
-----------------------------------------------
✅ Root cause found: Timeloop's wire model is a stub (pat.cpp WireEnergy returns 0) and no
   arch supplied coefficients, so every network cost 0.00 -- in the mapper's objective too
✅ archs/_shared/noc.yaml: one shared wire constant (0.12 pJ/b/mm, Keckler/Dally IEEE Micro
   2011 Table 1; first pass used 0.4, a repeated-global-wire upper bound) + per-design
   switching (v1 multicast controller `calibrated` to Eyeriss ISCA'16 Table IV "inter-PE =
   2x MAC" -> 0.9152 pJ/ingress at wire 0.12; v2 + Simba-like routers 0.25 pJ PER FLIT, v2
   at 3 operands/flit and ONLY at the router-cluster level; WS/OS/IS one explicit generic
   model) + per-design, per-level `noc_levels` naming which spatial containers ARE the NoC
✅ Injected into the arch the mapper sees (archs._inject_noc), so NoC cost is inside the
   optimiser; fingerprint + cache slug (`noc`) move with it -- no pre-NoC mapping is reused
✅ Three injection traps found and fixed, each measured: coefficients on storage components
   land on zero-hop networks; container attributes inherit onto intra-PE scratchpads (94% of a
   45% "NoC"); nested lane containers inherit the parent's router term (v2 SIMD, 750 fJ/MAC)
✅ parse_stats reads the Networks section (and no longer lets it leak into DRAM); "NoC" is a
   plotted category (grey, under Compute); Local key renamed `Local (spads/RF)`; CSV follows
✅ recon arm scales the WEIGHT share of NoC by K/N (decision 2026-09-07), recorded as an
   approximation on every result
✅ validate: every design must have a cited noc.yaml entry, may not declare its own wire
   constant, and `noc_levels` names must exist; per-result checks noc_energy_costed and
   noc_share_within_published_band_eyeriss_v2 (6-10%, JETCAS 2019)
✅ Offline tests: eccenergy/tests/test_noc.py (11), fixture = real stats.txt with networks
✅ Second pass (user: "v2 NoC is insane"): decomposed v2's 15.4% -> wire 74% / router 26%;
   6-variant ablation on the dev pair (FINDINGS.md). Adopted: wire 0.12 + cluster-only
   per-flit routers -> v1 3.8%, v2 5.3% (415.5 uJ vs 402 pre-NoC). Rejected: 8x2 cluster
   geometry -- NoC -18% but TOTAL +7% via a changed mapspace under the bounded search
⚠️ v2 at 5.3% sits just below the paper's 6-10%: sparse-run share (pushes ours up) vs 65nm
   wire (pushes ours down); consistent, not confirmed. Band check kept, detail says how to read
⚠️ router 0.25 pJ/flit is an ASSUMPTION (no published pJ/router for either NoC); v2 per operand
   0.083 via its 24b ports, Simba-like 0.25 (no published packing). ECC_NOC_ROUTER_PJ sweeps it
⚠️ Known over-counts kept and documented: 1-D cluster row (unicast 7.5 hops vs ~4 on 8x2) and a
   single edge-injected GLB where the chip has 16 distributed GLB clusters
⚠️ GLB->array and DRAM->GLB links carry no wire cost (zero hops in Timeloop's model); the
   interconnect modelled is the PE-array fanout, which is what the papers call the NoC
✅ All three generic dataflows exercised end to end with NoC (job 41295683): WS 7.7%, OS 6.0%,
   IS 4.8% of Timeloop total on the dev pair; 2-D hop counting confirmed working
✅ Mapper cache is race-safe: timeloop.ShapeLock (atomic mkdir + owner file, stale takeover), so
   (arch, model) tasks that share layer shapes may run concurrently; the second waits and reuses
✅ hpc/map.sbatch, eval_panel.sh, sweep.sh now share ONE env-overridable set of mapper defaults
   (random_pruned, search_size EMPTY = uncapped, victory 2000 depth-scaled, timeout 2000);
   eval_panel.sh used to hardcode 20000/2000 and silently miss any other cache slug.
   map.sbatch --time raised to 24 h for uncapped runs. hpc/tasks_full.txt (64) + tasks_conv.txt
✅ One-command pipeline: hpc/run_all.sh = map array + dependent hpc/eval.sbatch (evaluate +
   draw when the array succeeds); README quick start rewritten around it
❌ Full 8-model x 8-arch cold run with NoC not yet done; results/figures/ArchitectureSweep.png
   on disk is still the PRE-NoC result and must not be compared against NoC numbers


TASK 2  --  EMBEDDED ECC (DRAM effect only)          ✅ DONE 2026-09-07
------------------------------------------------------------------
✅ Questions answered first: no re-mapping needed (the mapper never sees parity; both arms are
   8-bit weights to it; plan requires the fixed mapping) and the baseline DRAM already carries the
   accounted external parity since Task 1 (10.5 stored b/weight at BCH(63,51), not flat n/k)
✅ ACTUAL embedded-codeword layout read from the embedding code (ECC-CODE-Engine 4-EmbeddingECC,
   copied into ../Input_Embedding/3-Testing): MSB-first weight bit stream cut into fixed 63-bit
   chunks, weights straddle (7.875 weights/codeword, period 8 cw = 63 weights, 7 of 8 boundaries
   split a weight), last chunk zero-padded, parity overwrites the n-k lowest-significance
   positions (BCH(63,51): every LSB + 52% of bit1). eccenergy/embedded.py; source cited in JSON
✅ Storage = 8 b/weight + one tail pad per tensor; external parity = 0; complete codeword read
   (no K/N cut at DRAM); DRAM energy = Timeloop's; only the external-parity term is removed
✅ Experiment `bash run.sh embedded --eval` (experiments/embedded.py): baseline + embedded
   evaluated in ONE JSON from the same cached mappings; recon placements stay not_implemented
✅ Checked, not asserted: non_dram_components_match_task1_baseline, dram_difference_is_exactly_
   the_external_parity, embedded_reads_complete_codeword, embedded_layout_hand_check,
   embedded_matches_sweep_figure_arm, evaluation_only_rerun, fixed_mapping_shared_across_variants
   -- all PASS on 12 whole-model + 6 dev-pair files
✅ Evaluation-only reruns from the cache: 0 newly mapped shapes on every file
✅ Stored bits vs physical reads vs energy kept apart and reported; bit-proportional DRAM word
   estimate and uncharged tail padding recorded as approximations on every file
✅ ECC codec energy explicitly OUTSIDE the comparison (charged to neither arm; warned if ECC_DECODE=1)
✅ Baseline code untouched (parity.py, external_parity, build_stacks, baseline.py); `baseline --eval`
   re-run after the change reproduces all 12 whole-model files field-for-field
✅ Whole-model saving at BCH(63,51): resnet18 5.5/5.6/5.2/4.1/3.2/2.8 %, mobilenet_v2
   3.8/4.1/3.6/3.4/3.4/2.6 % (v2, v1, Simba-like, IS, WS, OS) = parity / (Timeloop + parity)
✅ Tests: eccenergy/tests/test_embedded.py 11/11 (2 need pandas -> container); other suites unchanged
✅ hpc/eval_panel.sh writes the Task 2 file per model; hpc/summary.py --field embedded|saving
⚠️ The sweep figure's embedded DECODE count still uses the baseline's 6 weights/codeword
   (ECC_EMB_WEIGHTS_PER_CW); a codec-energy question, deliberately left for when codec energy enters
⚠️ eyeriss_v2 noc_share_within_published_band still FAILS (5.3% < 6%) -- pre-existing, same in Task 1
❌ No inference-accuracy claim (LSB overwrite) -- Input_Embedding's question, not this one's


TASK 3  --  EYERISS V2 RECONSTRUCTION, EVALUATOR ONLY, FIXED MAPPING   ✅ DONE 2026-09-07
----------------------------------------------------------------------------------------
✅ Architecture / model / layers / BCH / precision / mapping all FIXED; only the boundary varies
   (env.sh section 4 takes precedence over section 3 when ECC_RECON_MODELING=1)
✅ Mapping optimiser NOT re-run: RECON_OPTIMIZER=False is the only accepted value, `True` stops
   the run naming Task 4 rather than mislabelling fixed-mapping numbers. `evaluation_only_rerun`
   and `fixed_mapping_shared_across_variants` are checked on every result file
✅ No new arch YAMLs needed: a placement changes only how the evaluator charges the weight path,
   so the mapping fingerprint is unchanged and the whole Task 1/2 mapper cache is reused as-is
✅ All five weight-path boundaries of 01_..._architectures.txt §5.2 evaluated: R1 source/NoC
   ingress, R2 destination cluster, R3 SPad input, R4a SPad output repeated, R4b SPad + reuse reg
✅ Weight path READ from the model, not assumed: WEIGHT_PATHS names the Timeloop levels, and
   `weight_path_reconciles_with_raw_record` re-derives per-category weight energy from the
   cached stats and compares it with the Raw record. eyeriss_v2_like has NO weight GLB (its GLB
   banks are iacts+psums), so there is deliberately no global-buffer boundary
✅ Reduced representation applied only AFTER ECC correction: DRAM is the embedded arm's number
   to the digit, checked per placement (`dram_identical_to_embedded_reference`)
✅ Encoder work counted from ACTUAL reuse/fanout: total access counts (per-instance x utilized
   instances) at each boundary -- 13.85M DRAM reads, 13.85M mesh ingresses, 21.9M SPad fills,
   1.81 BILLION SPad reads. The 82.85x SPad read/fill ratio is the whole result: it is what
   R4a pays and what R4b's retained reconstruction amortizes away
✅ Encoder + buffer/control costs charged: reconstruction 4.1296 pJ/codeword (DC, BCH_63_51_t2)
   and R4b's register at 0.0328 pJ/write from this design's OWN Accelergy ERT. `Recon overhead`
   is its own plotted category so the overhead is visible, not hidden inside `Reconstruction`
✅ Physical packing explicit (§16): ECC_RECON_PACKING=stream|aligned. `aligned` costs the SPad
   its entire saving -- ceil(8x51/63)=7 bits, floor(24/7)=3 values per 24-bit word, same as at 8
✅ ECC-group dependency explicit (§15): G_rec=9 computed from the layout, not assumed
✅ Infeasible local placements REJECTED, not estimated: mobilenet_v2's 7 depthwise layers hold 3
   weights per PE, so R4a/R4b are `unsupported` there with the layers named
✅ No capacity-driven reuse or new tiling credited; no operand-retention saving credited to ANY
   bar (R4b's register would cut SPad reads, but so would the same register in a baseline PE --
   the comparison the plan asks for), so read counts are Timeloop's everywhere
✅ Table + energy-breakdown plot: results/tables/ReconSweep.csv, results/figures/ReconSweep.png,
   with reconstruction counts, overheads, totals and savings vs BOTH references. Labelled
   FIXED MAPPING on the figure itself
✅ Evaluation-only reruns for cost changes: `bash hpc/run_all.sh --replot` redraws in seconds
✅ Tests: eccenergy/tests/test_recon.py 15/15 on a synthetic stats.txt/map.txt (no cache, no
   container needed), two of them pinning the corrected R4b behaviour in BOTH directions;
   test_results_store / test_embedded / test_noc / test_mapper_lock unchanged
✅ Task 1 and Task 2 verified UNCHANGED: 5,075.3712 uJ and 4,798.3746 uJ before and after.
   ecc.py, parity.py, embedded.py, experiments/baseline.py, experiments/embedded.py untouched
✅ R4b WINS and beats embedded-only ECC. resnet18 vs embedded: R1 -0.15%, R2 -0.06%,
   R3 -0.09%, R4a -18.21%, R4b +1.36% (+6.75% vs conventional). R4b captures 65.3 of the
   77.5 uJ available = 84% of the ceiling. Ordering matches the source's ratings:
   R4b (5/5) > R2/R3 (4/5, break-even) > R4a (3/5). R1 worse than embedded is CORRECT (2/5)
✅ The CEILING is printed first on every run, so a small saving reads as arithmetic and not as
   a missing term: on-chip weight energy 407 of 4,798 uJ = 8.5%, x(1-K/N) = 77.5 uJ = 1.62% is
   the most ANY boundary can save; 91% of it is the SPad; DRAM's 18.5% is not reducible at all
❌ FIXED AFTER REVIEW -- the first version of this model reported R4b as the WORST boundary.
   The reuse model asked how many uses one weight gets IN A ROW (what a 1-entry latch serves),
   which the loop nest says is 1 on every mapping here, so R4b came out as R4a plus a register
   it never used. But Sec. 5.4's EV2-C is "reload/reconstruct when the required weight CHANGES"
   -- a RETAINED reconstruction, so the question is CAPACITY, not consecutiveness. The loops
   below the SPad walk a tile of 16-256 weights (== the SPad's utilized capacity on all 12
   resnet18 shapes) and repeat 1-6272x, so a register covering that tile reconstructs once per
   FILL: 82.85x fewer than R4a and the SAME count as R3. R4b is R3's encoder count with R4a's
   storage saving, which is exactly why the source rates it above both.
   Now: recon.retention_model() + recon.weight_loop_nest(), which report inner_tile,
   consecutive_run, innermost_weight_dim and stream-consecutiveness SEPARATELY
✅ ECC_RECON_REUSE_REG_ENTRIES (default `tile`) makes the capacity a knob. `1` (a latch) or `9`
   (a group buffer) collapse R4b back onto R4a at -19.45%, because a cyclic walk is the LRU
   worst case and there is no partial hit rate -- that is a real case, not the default one
⚠️ R4b's +1.36% is contingent on TWO things, both measured: `stream` packing (under `aligned`
   it becomes -0.19%, because floor(24/7)=3 seven-bit values fit a 24-bit word, the same 3 as
   at 8 bits) and a register covering the working set (a latch gives -19.45%)
⚠️ R4b's real overhead is CAPACITY, not energy: the register needs up to 256 full-width weights
   per PE against a 288-weight SPad, so the PE holds 6.48b reduced + 8b register = 14.48b per
   resident weight vs the baseline's 8b -- 1.81x the PE weight storage. Only its WRITE energy
   is charged (0.718 uJ); area and leakage are not modelled and the required capacity is
   reported per layer so the omission is visible
⚠️ CSC metadata not modelled (design is modelled dense), so R4a/R4b's SPad saving is an upper
   bound on what a CSC v2 would see. Warning on every result file
⚠️ The idle term is 54% of the reconstruction energy (2.2301 of 4.1296 pJ/codeword).
   ECC_RECON_INCLUDE_IDLE=0 halves it and still does not rescue R4a (437 uJ vs a 77 uJ saving)
⚠️ eyeriss_v2 noc_share_within_published_band still FAILS (4.6% < 6%) -- pre-existing, same in
   Tasks 1 and 2
❌ FOUND, NOT FIXED: archs/_shared/noc.yaml declares v2's intra-cluster level router_pj: 0.0 but
   the flattened architecture carries router_energy: 0.0833 on inter_PE_spatial (6.9 of the
   cluster-local stage's 13.7 uJ is switching). Affects every bar equally and scales identically
   under `stream`, so it does not bias this comparison, but the declaration and
   archs._inject_noc disagree and one of them is wrong
❌ Latency and area overhead not reported -- Task 3 asks for energy; Task 4 asks for those
❌ No inference-accuracy claim -- Input_Embedding's question, not this one's

   -- TASK 3 REVIEW (2026-09-07, second pass): "why is no reduction visible?" --
✅ (A) HELD. The K/N reduction IS applied, to every reducible stage of every boundary, exactly
   (`before x K/N` to 4e-16 relative) and to no other stage (untouched stages compare `==`).
   Measured on the REAL cache: mesh 24.158 -> 19.557 uJ at R2/R3/R4a/R4b; cluster_local
   13.694 -> 11.085 at R3 onward; weight_spad 369.276 -> 298.938 at R4a/R4b. Category NoC
   219.718 -> 215.117 (R2) -> 212.508 (R3); Local 631.838 -> 561.499 (R4a/R4b). The asker's
   arithmetic was right: R2 saves 4.602 uJ on the mesh and R3 a further 2.608 -- 0.096% and
   0.150% of a 4,798 uJ bar, which is why nothing shows
✅ 7 property tests added, each run on the SYNTHETIC stats AND the real mapper cache the
   published figure came from: per-stage exactness, non-weight-untouched, monotonicity down
   the path (the one that would catch "R3's NoC is not below R2's"), the closed-form identity,
   nothing-goes-up, a K sweep over 57/51/45/39/36/30, and an INDEPENDENT re-scan proving every
   weight-carrying level is claimed by exactly one stage. test_recon.py 15 -> 26, all pass
✅ MUTATION-TESTED (6 deliberate breakages of recon.py, each reverted): SPad reduction dropped
   -> 7 tests fail; network scales forced to 1 -> 5; scale hard-wired to 51/63 ignoring K -> 1
   (the code sweep ALONE catches it); R2 reducing the wrong stage -> 2; a silent 1% DRAM credit
   -> 4; the mesh saving applied twice -> 4
❌ FIXED: eyeriss_v2_like_wglb's weight path has a reducible `weight_glb` stage that NO
   placement reduces, so R2/R3/R4a/R4b would each have reported it at FULL width while still
   claiming their own savings -- every boundary understated, silently. feasibility() cannot see
   it (it only inspects stages a placement DOES claim). recon.validate_placement_space() now
   enforces the invariant the physics implies -- `reduced` must be a PREFIX of the reducible
   stages in path order, and every reducible stage must be reached by some boundary -- recorded
   as `placement_space_covers_the_whole_weight_path` on every result and raised in run().
   eyeriss_v2_like passes; wglb now fails loudly. No eyeriss_v2_like number moves (console
   output byte-identical before and after)
⚠️ (B) DID NOT HOLD as stated. The five weight-path numbers are right for what the model
   contains, and none of the three candidate missing terms rescues the figure:
   1. DRAM-to-accelerator LINK: CANNOT be separated. Accelergy's CactiDRAM is a flat
      `type2energy["LPDDR4"]=8 pJ/bit x width` using only (type, width) -- its own log says
      "Arguments used: (type, width)". No array/IO split exists to peel off, and Timeloop
      charges the DRAM<->chip network zero besides. Recorded as a LIMITATION, not guessed.
      Sized so the omission is visible: a 10/25/50% link fraction would lift the ceiling from
      1.62% to 1.97/2.50/3.38%. And NO boundary here would claim it -- R1..R4b all put the
      encoder at or below the accelerator's NoC ingress, downstream of the link. A boundary
      that shrinks it ("correct at the memory controller, ship K/N bits") is a SIXTH boundary
      and a different partitioning assumption; it also offsets every bar equally. Task 5
   2. WEIGHT NoC is NOT under-counted, and would barely matter if it were. Weight multicast on
      the mesh is 1, 2 or 4 across the 12 resnet18 shapes (not "1, scattered"); weight
      ingresses equal the DRAM weight reads exactly (13,849,792), the floor. Weight NoC is
      0.79% of the run; scaling the whole NoC to the published band moves the ceiling from
      1.616% to 1.641% (x1.330) or 1.711% (x2.315)
   3. GLB: eyeriss_v2_like genuinely has NO weight GLB -- confirmed level by level from the raw
      record (iact_glb=Inputs, psum_glb=Outputs only), so 0.00 uJ is correct, not a gap
⚠️ noc_share_within_published_band FAIL may be a DENOMINATOR question, not the coefficients:
   audit.py divides by raw.total, which INCLUDES DRAM (4.579%). Against ON-CHIP energy alone
   it is 219.718/(4798.375-1433.441) = 6.53%, INSIDE the paper's 6-10% band. Which denominator
   JETCAS Fig. 18 uses is a fact about the paper and is NOT settled here (no copy in this
   repo). Verdict left unchanged -- the check is Task 1's and Task 1's code is frozen
❌ STILL NOT FIXED, mechanism now known: archs._inject_noc writes `router_energy` only
   `if router:`, so a DECLARED ZERO is never emitted and inter_PE_spatial inherits its parent's
   0.0833 pJ (confirmed in the flattened architecture). Size: 44.94 uJ = 20.5% of the whole NoC
   and 0.94% of the run; 6.80 uJ of it is weight energy. Correcting it puts the NoC at
   174.78 uJ (3.68% incl DRAM, FURTHER below the band) and cuts R3's saving 7.210 -> 5.914 uJ.
   Not fixed here because it moves the mapping fingerprint (whole cache cold) AND Tasks 1/2's
   frozen 5,075.3712 / 4,798.3746 uJ. One line; belongs in the next cold mapping pass
⚠️ eyeriss_v2_like_wglb remains UNMEASURED (no mapper cache). Measured cost of a cold
   --map-only at the current settings, from SLURM job 41296638: eyeriss_v2_like x resnet18
   (12 shapes) = 2h26m on 18 cores; x mobilenet_v2 (31 shapes) = 3h00m. wglb adds a storage
   level and ECC_VICTORY_SCALING=levels raises the budget with depth, so 2.5-4 h (~45-70
   core-hours) for resnet18 is a LOWER bound. Worth doing -- it is the only way to measure a
   global-buffer boundary -- but quote it as a BOUND, not as Eyeriss v2. NOT LAUNCHED: a cold
   run needs an explicit go-ahead
✅ FIGURE WIDENED (approved, then made). A 4.60 uJ change on a 4,798 uJ bar is ~1 pixel, so
   ReconSweep.png showed five boundaries that looked identical. plots/stacked.py::draw_panel
   gained TWO PARAMETERS and is still the only routine that draws a bar:
   (a) `bar_notes` -- one caller-supplied line per bar; the placement figure prints the uJ
       moved on chip and the uJ paid for reconstruction, because a percentage cannot separate
       R2's 0.096% from R3's 0.150% and those two numbers ARE the result
   (b) `grouped_stacks(zoom_stacks=...)` -- a SECOND panel drawn by calling draw_panel again
       (as panels.py already does), same groups/palette/legend, its own unit. Scoped to the
       on-chip weight path: 407.1 uJ full width -> 402.5 (R2) -> 399.9 (R3) -> 336.8 (R4a/R4b),
       so the SPad saving is 19% of the panel instead of 1.5% of a bar. Excludes DRAM (not
       reducible) and the recon cost (it belongs to the bar above) uniformly, and says so
   Still ONE image, one table, one manifest. THREE SWEEPS UNTOUCHED: `run.sh --replot` and
   `ECC_SWEEP=bch ... --replot` reproduce ArchitectureSweep.png and BCHsweep.csv BYTE-FOR-BYTE
   under the new draw_panel (md5 before/after)
✅ test_recon 26/26, test_results_store / test_embedded / test_noc / test_mapper_lock all pass;
   `run.sh validate` PASS (6 architectures). test_recon also now runs clean on the HOST python
   (skips the pandas cases instead of failing -- Path.write_text(newline=) is 3.10+, and three
   tests imported pandas-backed modules before their own pandas guard)


TASK 4  --  EYERISS V2 RECONSTRUCTION + MAPPER       ❌ not started
   Next up. RECON_OPTIMIZER=True is the switch and currently refuses, by design. Two things
   §15 points Task 4 at: (1) R4b already needs 1.81x the baseline's PE weight storage, so a
   reconstruction-aware mapper has LESS headroom than the reduced SPad alone suggests -- the
   question is whether it can shrink the working set the register must cover; (2) R4b's whole
   saving rests on `stream` packing, and representing the packed width per level in the mapper
   is where that stops being an assumption.
TASK 5  --  OTHER ARCHITECTURES                      ❌ not started
   Add WEIGHT_PATHS[<name>] and PLACEMENTS[<name>] to eccenergy/recon.py TOGETHER, then
   ECC_RECON_MODELING=1 ECC_RECON_ARCH=<name> bash run.sh recon --eval


CONFIGURATION  --  ONE env.sh, ONE COMMAND                ✅ DONE 2026-09-07
------------------------------------------------------------------
✅ env.sh at the project root is now THE knob file: every ECC_* variable, one per line with a
   comment, in ten sections (1 most-changed, 2 mapping optimiser, 3 what the pipeline runs,
   4 recon placement study (Task 3), 5 hardware, 6 ECC accounting, 7 cluster, 8 output, 9 misc,
   10 derived). Sourced by run.sh, hpc/run_all.sh, hpc/map.sbatch and hpc/tl.sh
✅ Removed the four hand-synchronised copies of the mapper defaults (run.sh had
   hybrid/500/10000, map.sbatch + eval_panel.sh + sweep.sh had random_pruned/2000/2000): the
   evaluator could read a different cache slug than the mapper wrote. One copy now, in env.sh,
   defaulting to the converged HPC values -- verified to resolve to the existing uncapped cache
   (multimodel__opt-energy__vic2000__vicx__alg-random_pruned__to2000__noc__paper, 43 mappings)
✅ Section 3 master lists (ECC_ARCHS, ECC_MODELS, ECC_KS, ECC_APPROACHES) replace
   ECC_SWEEP_*/ECC_CONST_*/ECC_PANEL_MODELS/tasks_*.txt as the editable surface; section 10
   derives config.py's swept-list-plus-two-constants form, the panels layout, ECC_STEM, and
   GENERATES hpc/.runtime/tasks.txt, so a stale --array can no longer skip pairs silently
✅ hpc/run_all.sh is the single command: --map-only / --eval-only / --replot / --local; it
   submits itself as the dependent eval job, sized and resourced from env.sh section 7
✅ New ECC_RERUN_OPTIMISER: re-solves a shape even on a valid cache hit and overwrites it
   (config.py + timeloop.py::stats_for, one re-map per shape per process); refused together
   with ECC_FROM_CACHE, which forbids mapping outright
✅ Deleted: hpc/eval_panel.sh, hpc/eval.sbatch, hpc/sweep.sh, hpc/tasks_{panel,full,conv}.txt.
   run.sh is now a 51-line entrypoint with no knobs; map.sbatch keeps only its #SBATCH header
✅ Verified: `bash hpc/run_all.sh --eval-only` reproduces results/tables/ArchitectureSweep.csv
   and the figures byte-for-byte (git diff empty); test_results_store, test_embedded,
   test_mapper_lock pass; test_noc's one failure is the pre-existing missing-pandas import
✅ Section 4 (recon placement study) is LIVE as of Task 3: ECC_RECON_MODELING switches the whole
   pipeline to it and section 4 then takes precedence over section 3. New: RECON_OPTIMIZER (the
   Task 4 placeholder, refused if True), ECC_RECON_PACKING, ECC_RECON_ENCODER_GRANULARITY,
   ECC_RECON_REUSE_REG_ENTRIES, ECC_RECON_REUSE_REG_PJ, stem ReconSweep. Removed: ECC_RECON_PLACEMENT_LABELS (a property of
   the weight path -- it lives with it in eccenergy/recon.py), ECC_RECON_RESULTS_JSON and
   ECC_RECON_RERUN (the results go through the normal result store). ECC_RECON_PLACEMENTS is a
   bash associative array and cannot be exported, so section 10 flattens the entry for
   ECC_RECON_ARCH into ECC_RECON_PLACEMENT_LIST, which is what config.py reads
✅ Pipeline verified on SLURM, not just locally (jobs 41332209 map array + 41332210 eval):
   12/12 map tasks COMPLETED (~5 s each, raw-cache hits), dependent eval job COMPLETED in 56 s
   and rewrote results/figures/ArchitectureSweep.{png,pdf} + tables + manifest; git diff on
   results/ is EMPTY, so the rebuilt output is byte-identical to the committed one
❌→✅ TWO defects found by that verification, both fixed:
   1. ECC_RERUN_OPTIMISER was UNREACHABLE. energy.py::collect() returns on a raw-energy cache
      hit BEFORE mapper_factory() is called, so the flag did nothing on any (arch, model)
      already evaluated once. A re-map request now invalidates the raw record too, and
      ECC_REPLOT_ONLY joins ECC_FROM_CACHE as a refused combination (neither builds a mapper).
      Proven on job 41331682: cold -> mapped; default -> "raw cache hit" (mapper never
      reached); RERUN=1 -> raw cache ignored, "re-solving ... overwriting its cache entry"
   2. The dependent eval job died in 0 s (job 41331853): `./env.sh: No such file or directory`.
      sbatch executes a COPY at /var/spool/slurmd/job<id>/slurm_script, so ${BASH_SOURCE[0]}
      pointed outside the project. hpc/run_all.sh now resolves the root from its own location,
      then an inherited ECC_PROJECT_ROOT, then SLURM_SUBMIT_DIR, and exits with a clear
      message if none holds an env.sh. map.sbatch was never affected (absolute cd)

2026-09-08  NoC REVISION (FINDINGS.md §4.1) -- implemented, tests + validate pass, unmapped
✅ E  archs._inject_noc writes router_energy / energy-per-ingress on every charged level, zero
      included (v2's PE row no longer inherits the cluster router: -29.7 uJ fixed-mapping)
✅ C  noc.yaml `tile_width_um` per level -> injected `tile_width`; v2 mesh hop 430 um (PE cluster
      + GLB cluster + 2.6% routers), PE row 108 um (= Timeloop's own; declared so it does not
      inherit). validate refuses a partial declaration. test re-derives both from the cached ART
✅ A  noc.yaml shared.pe_latch_pj 0.5 on v2's PE row (`ingress_pj: pe_latch`), bracket 0 / 0.9152;
      ECC_NOC_PE_LATCH_PJ overrides, slug `nocl<v>`, in the fingerprint
✅ D  eccenergy/noc_post.py: spatial reductions (Aladdin adder 0.0065625 pJ/bit x psum width + one
      psum-wide hop) and Outputs wire re-billed 8b -> accumulator width. EVALUATOR-ONLY (Timeloop's
      pat::AdderEnergy is a stub and word_bits is per network). Stamp on every Raw; load_raw
      rejects a mismatch. timeloop._parse_networks now carries reductions/hops/per-hop/instances
✅    Register writes: aladdin_register.csv has write = 0 pJ. regfile_decoded write/update charged
      at the read row; new register_rw.yaml for the simple_* bare registers (arch_paper.yaml
      updated). archs.components_digest() is in arch_fingerprint -> EVERY cache cold
✅    audit.py: v2 band read against on-chip energy, both shares reported
❌    Not mapped. Smoke mapping (single layer, bounded) of v2 + output-stationary: see §4.1.1
      Decision pending: ECC_OPT_METRIC=edp (user leaning yes; NoC latch to be re-read after it)
✅    Smoke mapping (SLURM 41452254/41452255, one layer, ss400/vic100): tile_width honoured (412.80 /
      103.68 fJ per hop), PE row router 0 / ingress 0.5, psum_spad write=update=read=0.13263,
      register_rw compiles, evaluator rows + stamp present, baseline --eval hand check PASS
❌    NEW open defect 8.7: Neurosim plug-in fails (read-only image FS) and Accelergy takes its 0 pJ
      for some address generators -- present in every cached run since the image was built

2026-09-09  FIRST EDP MAPPINGS -> NoC 16-19% of on-chip on 11 layers; diagnosed (FINDINGS 4.1.2)
✅    mesh wire 3x (1-D 16-row, EDP fills all clusters, 7.5-13 mean hops) = the error; latch additive only
✅    v2 PE clusters now 8x2 (arch_paper.yaml), no forced split; pe_latch_pj -> 0; band vs mesh only
      (noc.yaml paper_band_levels, audit.py mesh_share_of_onchip) -- ASSUMPTION about Fig. 18 to verify
✅    hpc/map_by_shape.sh: one SLURM job per shape + dependent eval; 12 jobs 41474947-58, eval 41474959
      -> results/figures/ReconSweep.png (8x2, EDP, latch 0). 1-D full run 41454163/64 kept -> *_1Drow_latch0.5
✅    legacy/plot_unfinished_recon.sh: temporary partial-layer figure from whatever is mapped (ReconSweep_unfinshed)
❌    cancelled 41454165-68 (latch 0 / 0.9152 brackets: identical mappings, no longer needed)

2026-09-09  8x2 WHOLE MODEL DONE -> results/figures/ReconSweep.png (FINDINGS 4.1.3)
✅    12 per-shape jobs 41474947-58 all mapped (14 min .. ~35 min each); eval re-run from the host
      total 5940 uJ; NoC all 626 uJ = 14.9% on-chip; mesh only 249 uJ = 5.9% on-chip (band lower edge)
❌→✅ two incidents: map.sbatch's own --array made per-shape jobs 12-task arrays (eval afterok never
      satisfied) -> map_by_shape.sh passes --array=0-0; a queued eval picked up the edited YAML's
      fingerprint and wrote a 1-layer 'layers-full' record that the next eval reused -> deleted,
      energy.load_raw now rejects records with unmapped layers (tested)
❌    ReconSweep_1Drow_latch0.5.* was that 1-layer figure; deleted. 1-D raw (fp-3eb860ea2b2a) is intact
❌    R4a/R4b unsupported on resnet18 under EDP (layer4 keeps < 9 weights resident per PE)


2026-09-09  TASK 5 — WEIGHT-STATIONARY AND EYERISS V1 JOIN THE PLACEMENT STUDY
✅    recon.py: WEIGHT_PATHS + PLACEMENTS for simple_weight_stationary (6 boundaries) and
      eyeriss_like (5), stage-to-level match read off real stats.txt/map.txt. v1 mirrors v2
      (2 network stages + PE spad, no weight GLB); WS is the only design with BOTH a weight
      global buffer above the network and a stationary weight register below the spad, so it
      gets §6.2's buffer-output row AND its MAC-input row
✅    recon6 (R5 @ MAC input) is listed so it is REPORTED infeasible, not omitted: weight_reg
      holds 1 weight, G_rec = 9. feasibility() rejects it and names the layers
✅    WS's existing depth-1 weight_reg is NOT R4b's reuse register: the mapping fills it once
      per read (reads == fills == 115.6M), so it is a pipeline latch. R4b's register must cover
      the inner tile (48 on layer4.0.conv2). CLAUDE.md's Simba rule answered, answer is no
✅    retention_stage() + weight_loop_nest(prefixes): the reuse register is priced against ONE
      named buffer. Unscoped, WS summed glb+spad+reg reads/fills and read inner_tile=1 off the
      depth-1 register for a mapping whose spad tile is 48. Both eyeriss designs have one
      storage stage -> no-op there, v2 ReconSweep.png byte-identical after the change
✅    ECC_RECON_ARCHS = one PANEL per design in one ReconSweep.png (plots/panels.stacked_panels
      widened with draw_panel's bar params, no second renderer). Boundaries still never share an
      x axis; each panel has its own axis, boundary list and reference bars
✅    write_table: a zero-height row no longer prints "total 0.0, saving 100.0%" beside
      "status unsupported" -- the derived pair is blank, matching the study's own columns
✅    map_by_shape.sh: several architectures in one wave + ONE dependent eval (a launcher per
      design would give each its own eval and overwrite the other's figure); --no-eval;
      ECC_LAYERS spot-check; the SLURM task file is snapshotted per submission
✅    tests: 39 recon ok / 1 skip; noc, embedded, mac_override, results_store, mapper_lock pass.
      New: every registered design's placement space is valid; the retention buffer is one
      named storage stage; several architectures are panels and an empty list is refused
✅    SPOT CHECK, 2 layers (layer1.0.conv1 + layer4.0.conv2), AFTER the multicast fix
      below -- NOT full-model numbers, and the pre-fix single-layer figures first
      recorded here were superseded within the hour:
      WS  R4b +7.24% / R3 +3.96% / R2 +3.61% / R1 +2.78% / R4a -7.78% vs embedded,
          R5 infeasible.  Ordering matches the source hypotheses:
          5/5 > 4/5 > 3/5 > control > 2/5, and R4a really does LOSE energy
      v1  R3 +4.13% / R1 +3.61% / R2 +3.07%; R4a/R4b infeasible (spad keeps 8 < G_rec 9)
          -> R2 falls BELOW R1 on v1: its column network multicasts 7-fold, so a
             destination-side encoder is replicated 7x and costs more than keeping
             that network reduced saves. Sec. 7.1's tradeoff, resolved against
             replicated encoders on this design
      Both designs: the boundary just after the LAST network (R3) dominates R2 --
      identical encoder count (a network's arrivals ARE the next stage's intake),
      strictly more saving
❌    v1's PE-local boundaries are ONE WEIGHT short under EDP -- the same mechanism that made
      them unsupported on v2. G_rec = 9 is fixed by n=63 over 8-bit weights, so the only lever
      is the mapping: hence two waves, EDP (jobs 41490547-70, eval 41490571 -> ReconSweep) and
      ECC_OPT_METRIC=energy (41490572-96, eval 41490597 -> ReconSweep_optEnergy), where the
      resident tiles are 128-192 and every boundary evaluates

2026-09-09  R2 DEFECT FOUND WHILE ADDING THE TWO DESIGNS: multicast multiplicity
❌→✅ A NETWORK boundary charged the network's INGRESSES while crediting that same
      network with carrying the reduced form. Those are two different placements:
      an encoder before the fanout makes the network full width, and that is R1.
      The count for R2 is the destination-side ARRIVALS, ingresses x multicast
      factor, summed exactly off Timeloop's own `@multicast M @scatter S` lines
✅    ECC_RECON_ENCODER_SITE=destination (the model) | source (the pre-fix row,
      kept runnable for the diff), the experiment variable Sec. 7.1 asks for
✅    Cross-check recorded on every result (ModelWeightPath.multicast_chain): a
      network's arrivals equal what the next stage takes in. On eyeriss_v2_like
      mesh 16,356,544 injected -> 23,812,480 arrivals = cluster_local ingresses
      = weight_spad fills, both networks match exactly
❌    IT CHANGES A PUBLISHED NUMBER. eyeriss_v2_like R2 vs embedded 3.87% -> 3.78%
      (recon 8.817 -> 12.837 uJ, N_rec 2,077,021 -> 3,023,807) and R3 (+3.84%) is
      now the best boundary instead of R2 -- because the mesh's arrivals equal the
      cluster-local ingresses, so R2 and R3 pay the SAME encoder count and R3
      saves strictly more. FINDINGS 7.x and PROJECT_STATUS quote the old ordering
✅    The old eyeriss_v2 figure/table/manifest are preserved as
      results/figures/ReconSweep__eyeriss_v2_2026-09-09.* before ReconSweep.png
      became the two-panel WS + v1 figure
✅    New test: a 3-fold multicast fixture -> arrivals = 3 x ingresses, both
      readings recorded on every network bar, the two differ by exactly 3x, and
      the encoder site does NOT change which stages carry the reduced form
❌→✅ INCIDENT: map_by_shape.sh took `--progress` before the flag existed, fell
      through its option loop and SUBMITTED A WHOLE DUPLICATE WAVE (24 map jobs
      + a second eval, 41491127-51) that would have overwritten the real eval's
      figure. scancel'd within a minute; the real waves 41490547-97 untouched,
      no stale locks (the duplicates never claimed a shape). Unknown flags are
      now FATAL in that script instead of falling through to a submission
✅    hpc/map_by_shape.sh --progress: per architecture, shapes SOLVED (a real
      timeloop-mapper.stats.txt) vs in-progress (a .lock) vs claimed (a
      directory). Counting directories is what made me report a wave finished
      seconds after it started -- a directory is created when a job CLAIMS a
      shape, not when it solves it

2026-09-09  BOTH WAVES DONE -> two figures, and the OBJECTIVE is the lever (FINDINGS 7.6)
✅    results/figures/ReconSweep.png (EDP, eval 41490571) and ReconSweep_optEnergy.png
      (energy, eval 41490597), both 21/21 layers, both two-panel
✅    WS is objective-insensitive: every bar within 0.05 pp, refetch 1.07x either way
✅    v1 under `energy`: spad residency 8 -> 20 clears G_rec 9, R4a/R4b FEASIBLE,
      R4b +11.38% vs embedded / +29.45% vs conventional = 92% of its 12.33% ceiling,
      the best result in the study; R4a -14.74% (230.4M reconstructions vs 3.7M)
✅    v1 total energy 6.290 -> 3.613 mJ under `energy`. Quote the PAIR, not one
❌→✅ CORRECTED an over-strong inference I had given the user: cutting refetch does cut
      the interface saving in uJ (212.81 -> 145.61, and the proportionality to DRAM
      weight reads is exact to 4 digits), but the PERCENTAGE ROSE 3.38% -> 4.03%
      because the objective cut non-weight energy 51.4% against the weight path's
      much smaller cut. The lever is the weight SHARE of inference energy, not the
      refetch factor -- measure the ratio, do not predict it from refetch alone
❌    NOT FIXED, WS design defects found in the utilisation audit (user asked for
      recommendations only, no code changes): PE array at 6.25% (16 of 256, meshX
      never used, 11 of 12 shapes); zero temporal loops below the register level on
      all 12 shapes so weight_reg serves 1.07 uses per load. Fixing the latter will
      shrink R4b's headline


2026-09-09  TASK 4 — CAPACITY DILATION: BUILT, AND THE ASSUMPTION MEASURED FIRST
✅    ECC_WEIGHT_CAPACITY_SCALE multiplies `depth:` on the WEIGHT-carrying levels of the
      arch the MAPPER sees; ECC_WEIGHT_CAPACITY_SCOPE=exclusive|shared brackets a level
      that holds Weights beside another dataspace (WS operand_glb). A declared depth-1
      register is never scaled -- it is a latch, and scaling it invents R4b's reuse level.
      Each scale is its own mapper cache (wcap<scale>); a scale that rounds every weight
      depth back keeps the undilated one
✅    RECON_OPTIMIZER=True is IMPLEMENTED and is `Post` only. The refusal it used to be is
      kept where it can be checked: dilated_view() stops the run if the reconstruction
      arm's cache is missing, if the design has no weight level to dilate, or if the
      delivered capacity is not N/K x the reference's. Both crossed phase/optimizer
      combinations are config errors, so --dry-run reports them
✅    Task 4 reduces EXACTLY to Task 3 when the mapper returns a byte-identical nest --
      R1 6.31 / R2 6.30 / R3 6.96 / R4a -7.57 / R4b 12.86 in both. That is not a shortcut:
      on an identical nest the recon arm IS the reference mapping on the same silicon
✅    Stem ReconSweep_optimiser, on a layer-scoped run too (env.sh section 10 and
      Config.stem both append it) -- a Task 4 figure can never overwrite a Task 3 one
✅    hpc/map_capacity_sweep.sh (design x layer x capacity, no dependent eval) +
      `bash run.sh dilation`, which diffs the per-layer DRAM weight reads. 102 mapping
      jobs submitted across 5 designs, 2 layers, 9 capacities, 2 objectives

❌→✅ THREE BUGS FOUND BY CHECKING TASK 4 AGAINST TASK 3 ON AN IDENTICAL MAPPING
      1. TWO SPELLINGS OF ONE CAPACITY. 63/39 is 1.61539 by python's %g and 1.6154 by the
         shell that submitted the wave: same fingerprint, two cache directories, and the
         evaluator refused a cache it had. Scale quantised to 4 dp everywhere
      2. THE CORRECTION MISSED THE DRAWN BAR. Task 4 came out WORSE than Task 3 (+8.61 vs
         +12.86) on a byte-identical mapping; the whole 18.3 uJ was Accelergy's 1.24x
         price for the deeper array leaking into the plotted category, which the
         weight-path-only correction never touched
      3. READS AND WRITES DO NOT SCALE TOGETHER (0.807 vs 0.725), and Timeloop counts
         SCALAR accesses while the ERT prices a VECTOR -- an absolute rebuild came out
         exactly 2x (the block size) and the correction refused itself. Now an
         access-weighted ratio, in which the block size cancels, guarded by a
         reconciliation that demands a whole number of values per physical word

❌    NOT CORRECTABLE, stated instead: where noc.yaml pins no tile_width_um, Timeloop
      derives the NoC hop length from the inner level's Accelergy AREA, so a deeper
      scratchpad also lengthens the wires into it -- 0.17 pp against the recon arm on the
      two-layer scope. With the ERT bias on the mapper's own objective, every Task 4
      number is a lower bound on three counts

❌    THE MEASURED ANSWER SO FAR IS ZERO, and the reason is mechanical, not statistical.
      On eyeriss_like's layer2.0.conv1 the declared and dilated loop nests are
      BYTE-IDENTICAL at every capacity from x0.125 to x1.6154. ifmap_spad is at 24/24 =
      100% while weights_spad is at 96/448 = 21% and the dilation takes it to 13%. The
      refetch of 8x comes from `for Q in [0:2) / for P in [0:4)` above the only
      weight-holding level, and those loops are there because the INPUT path ran out of
      room. Same on v2 (psum_spad), on WS (output_activation_reg at 1/1 = 100%, pe_spad
      at 12.5%) and on eyeriss_like_wglb (filter_glb 75%, ifmap_spad 100%).
      FINDINGS section 9 item 0 predicted 8.000 -> 5.333 on that layer; measured 8.000
❌→✅ FIXED two PRE-EXISTING test failures the Task 5 commit introduced and its message
      claimed passing: the closed-form and full-width tests spelled eyeriss_v2_like's
      stage names literally while reading whichever design ReconSweep.json names, which
      Task 5 changed to WS + v1. Both now derive from the design; 51 recon tests pass
❌    STILL FAILING, pre-existing and unrelated: test_noc
      test_declared_pitches_match_the_cached_art

2026-09-09  TASK 4 FIRST PASS WITHDRAWN -- THE OBJECTIVE CONFOUNDED IT
❌    The whole capacity sweep was run under ECC_OPT_METRIC=energy, which SERIALISES:
      v1 used 4, 8 or 56 of its 168 PEs and WS 16 of 256, DEPENDING ON THE CAPACITY.
      So PE count moved up to 14x while capacity moved 17x, and no capacity effect can
      be separated from that. Caught by building the utilisation table the user asked
      for -- the `PEs used` column is what showed it; the earlier tables did not have it
❌    Cancelled the 7 remaining energy-objective jobs (41495170-72 converged search,
      41513017-20 upward sweep). Their findings are not valid and FINDINGS 7.7 is marked
      WITHDRAWN rather than quietly corrected
✅    Re-running under edp -- env.sh's default, what ReconSweep.png and every Task 3
      number use, and it penalises serialisation so PE utilisation stays put:
      41513005-16 (3 designs x 2 layers x {1.6154, 4.0}) and an upward sweep
      {8, 16, 32} on v1 + v2 layer2.0.conv1 to find where refetch actually breaks
✅    WHAT SURVIVES, because it is arithmetic and not a measurement: on v1's
      layer2.0.conv1 only 5,376 of the layer's 73,728 weights are ever resident, a
      13.7x shortfall against N/K's 1.6x; and refetch is a product of integer loop
      factors whose smallest step here is 2x
❌    simple_weight_stationary is not a sound test bed until section 8's PE-utilisation
      defect is fixed (16 of 256 PEs, meshX never used, 11 of 12 shapes)


2026-09-09  TASK 4 UNDER EDP — BOTH POSITIVES WERE ARTIFACTS; REFETCH IS A LOOP-ORDER PROPERTY
❌    eyeriss_v2_like "refetch 14.00 -> 4.00 at x1.6154" WITHDRAWN: a CROSS-FINGERPRINT
      comparison. 14.00 is fp-3eb860ea2b2a (solved 09-08 23:49); the fingerprint the
      current config reads is fp-88656178371f (09-09 01:33) and it ALREADY refetches
      4.00 UNDILATED -- x1, x1.6154 and x4 nests are byte-identical. Two fingerprints
      under one variant slug are two ARCHITECTURES. The PEs 192->144 was the tell
❌    simple_weight_stationary "2.00 -> 1.00" WITHDRAWN: a LOOP PERMUTATION, not capacity.
      x1 `for Q(2) / for C(4)` vs x1.6154 `for C(4) / for Q(2)` -- the ONLY difference,
      and x4 goes back to 2.00. operand_glb holds 18,432 and pe_spad 24/PE at EVERY
      scale; nothing extra was stored. With C outermost the weight tile is invariant
      across Q so it is retained. That permutation was reachable at declared capacity
❌    THE ANSWER IS STILL ZERO, and now for a legible reason: `weights held` NEVER MOVES.
      eyeriss_like layer2.0.conv1 holds exactly 21,504 weights at x1, x1.6154, x4, x8,
      x16 and x32 -- room grows 32x to 0.9% fill and the mapper stores the same weights.
      Energy meanwhile rises 220 -> 454 uJ: Accelergy prices the deeper array as bigger
      silicon, the documented bias AGAINST the hypothesis
✅    THE MECHANISM. Refetch = product of the DRAM-level loop factors that do NOT index
      Weights and sit outside one that does: v1 Q(2)xP(4)=8, v2 Q(4)=4, WS Q(2)=2.
      A weight tile CANNOT INDEX P OR Q, so a weight buffer INSIDE the PE array can
      never absorb those loops however large -- which is exactly why v1 is flat to x32.
      Capacity binds only once the weight level is too small for the tile the chosen
      ORDER requires; at declared sizes the fullest level is an activation or psum buffer
✅    PREDICTION RECORDED BEFORE THE DATA. Two levels are weight buffers ABOVE the PE
      array and both hold 65,536 = 88.9% of layer2.0.conv1's 73,728 weights -- they just
      miss and N/K=1.6154 clears it. Break at x1.1250, money window ref in [0.6964,
      1.1250): eyeriss_v2_like_wglb weight_noc, and WS operand_glb under scope=SHARED.
      eyeriss_like_wglb filter_glb (8,192 = 11.1%) breaks at x9, window [5.5714, 9)
✅    ECC_WEIGHT_CAPACITY_SCOPE=shared is REQUIRED for WS: under `exclusive` operand_glb
      is never dilated (it holds Inputs too), so the x1.6154 run enlarged only a PE
      scratchpad that structurally could not matter
✅    136 EDP mapping jobs submitted (41520010-41520626, qos=rewetz-b): downward pairs
      refs 0.5->0.03125 on the 3 exclusive designs; WS under shared; and BOTH _wglb
      designs, which had NO EDP cache at all. All EDP, never energy
✅    THE INSTRUMENT, first. `dilation --table`: one row per swept capacity per design
      per layer with weights/PE, PEs, MACs used/declared, weight room, WEIGHTS HELD,
      fill%, DRAM weight reads, refetch, total uJ, fullest other buffer, saving, verdict
      and the FINGERPRINT. `room` and `held` sum EVERY weight level, so eyeriss_like_wglb
      is not described by its spad alone
✅    TWO GUARDS, each of which caught one of the above. sibling_fingerprints() enumerates
      every solved fp under one variant slug and marks the current one; capacity_verdict()
      says `capacity` only when reads fall AND held rises, `PERM?` when reads move while
      held is identical everywhere, `PE!=` when the PE counts differ
✅    eccenergy/tests/test_dilation.py, 8 tests, incl. 2 property tests on the REAL cache.
      THREE MUTATIONS RUN AND ALL CAUGHT: drop the held check, drop the PE check, read
      the fingerprint from the shape dir. Dropping the held check also trips the
      real-cache property test, because the WS pair on disk is the one it would misreport
❌    STILL FAILING, pre-existing and unrelated: test_noc
      test_declared_pitches_match_the_cached_art. Everything else passes in the container

2026-09-09  DOWNWARD SWEEP LANDING: EDP DOES NOT HOLD THE PE COUNT ONCE THE BUFFER IS SHRUNK
❌    THE HANDOFF'S PREMISE IS ONLY HALF TRUE. `edp` keeps eyeriss_like at 168/168 PEs at
      x1, x0.8077, x0.5 and x1.6154 -- but BELOW x0.5 it trades parallelism for capacity
      just as `energy` did: PEs go 168 -> 84 (x0.25) -> 96 (x0.125) -> 112 (x0.2019).
      So the downward sweep is CONFOUNDED below x0.5 and the objective is not a fix
❌    The two rows that look like wins are therefore unusable:
      x0.125 -> x0.2019  +14.13%, refetch 8.000 -> 4.000, held 1,152 -> 3,584 (a real
        rise!) -- but PEs 96 -> 112, so ~17% more compute is mixed into it
      x0.25  -> x0.4038  -7.58%, the EMBEDDED arm better: at x0.25 the mapper serialised
        to 84 PEs, drove ifmap_spad to 100% and found refetch 4.000, while the dilated
        arm kept 168 PEs and refetch 8.000. A SMALLER buffer refetching LESS
✅    WITHIN THE CLEAN RANGE (x0.5 and up, PEs pinned 168/168) EVERY PAIR IS FLAT.
      That is the honest result: zero where it can be measured without confound
✅    capacity_verdict() CHANGED: PE!= now SUPPRESSES the `capacity` label instead of
      sitting beside it (a row reading `PE!=,capacity` invites the 7.7 mistake twice).
      Exactly one verdict per pair; only `capacity` may be quoted. Reads direction still
      reported as PE!=,reads-fell / PERM?,reads-fell
✅    ECC_WEIGHT_FACTOR_RELAX BUILT (env.sh, config.py, archs._relax_weight_factors).
      Drops the factors: pins on the WEIGHT-INDEXING dims (M,C,R,S) of the temporal
      constraints on weight-carrying levels; N,P,Q keep theirs (weights do not index
      them, so relaxing those retiles activations instead -- a different experiment).
      Own cache slug `wrelax` + own fingerprint (92387e81b60c -> fd3a7b9f4847), with the
      same no-op rule as wcap. IT IS A DIFFERENT DATAFLOW: eyeriss v1's M=1 at the filter
      spad IS row-stationary, so a wrelax run must never be quoted as the published chip
✅    Verified per design: v1/v2/both _wglb drop `M=1/S=1` at weights_spad (factors
      [N=1,M=1,P=1,Q=1,S=1] -> [N=1,P=1,Q=1]); psum levels and the depth-1 latch
      untouched. simple_weight_stationary relaxes NOTHING -- its weight levels pin
      nothing a weight tile is indexed by, so this lever cannot help it, and it keeps
      its existing cache rather than cold-starting
✅    24 relax jobs submitted (41526422-45): v1+v2 x {layer2.0.conv1, layer3.0.conv1} x
      6 scales. 175 EDP jobs in flight in total
✅    BETTER TEST LAYERS FOUND from --survey. eyeriss_like conv1 refetches 224x; layer3.0
      .conv1 14x at 42.9% weight fill (the highest fill of any high-refetch layer, so the
      likeliest to bind); layer1.* 16x. layer2.0.conv1 -- the layer both passes used --
      is NOT the best test bed. 36 more jobs submitted on the better layers
✅    CEILING COMPUTED BEFORE MEASURING (layer-weighted, all 21 layers, declared cap):
      model refetch v1 1.868 / v2 1.401 / WS 1.072, so the MAX possible cut in DRAM
      weight reads at INFINITE capacity is 46.5% / 28.6% / 6.7%. Against Task 3's 0.152
      fixed-mapping efficiency that is 3.05x / 1.88x / 0.44x. ON WEIGHT-STATIONARY THE
      WHOLE TASK 4 PRIZE AT INFINITE CAPACITY IS 0.44x WHAT TASK 3 ALREADY BUYS
✅    11 dilation tests pass (7 real cached pairs checked), incl. 3 for the relax lever
❌→✅ RESOLVING A MAPPER-CACHE PATH USED TO CREATE IT. paths.mapper_cache() ended in an
      unconditional mkdir, so the read-only dilation report created a directory for every
      (design x capacity) it merely ASKED about -- 89 empty <slug>/fp-<hash>/ dirs, and an
      `ls` of eyeriss_like_wglb then showed 17 wcap variants where a few had been mapped.
      Fixed: mapper_cache(create=False) for readers; writers keep the default.
      The 89 dirs are LEFT ON DISK on purpose -- verified to contain nothing (no stats, no
      .lock, no sidecar), nothing reads them as a mapping, CLAUDE.md says never delete the
      cache, and 175 jobs were writing into that tree. `find ... -type d -name 'fp-*'
      -empty -delete` when the queue is idle if wanted
✅    THE ANSWER TO "HOW FAR DOES N/K FALL SHORT", measured on the one PE-MATCHED pair
      where capacity nearly binds. eyeriss_v2_like layer2.0.conv1, x0.25 -> x0.4038:
      192 PEs BOTH arms (no confound), embedded fill 88.9%, and the two loop nests are
      BYTE-IDENTICAL. 63% more room at 88.9% fill bought NOTHING.
      The arithmetic: resident tile = C(4) x M(8) x M(2 spatial) = 64 weights/PE. C=64 is
      factored 4 (psum_glb) x 4 (spatial-X) x 4 (spad), so the only growth is C 4->8, a
      2x TILE step to 128. At 3 weights/64b word that needs depth>=43 = x0.4479 = a
      x1.7917 dilation over the x0.25 reference.
      N/K = 1.6154 gives 117 weights/PE against 128 needed -- SHORT BY A FACTOR OF 1.109,
      eleven weights per PE.
      In code terms: BCH(63,39) 1.6154 no; BCH(63,36) 1.7500 NO (misses by 2.4%);
      BCH(63,30) 2.1000 yes. NO CODE WEAKER THAN BCH(63,30) BUYS THIS STEP, and that
      costs 3.3x parity overhead instead of 1.6x
✅    eyeriss_v2_like is otherwise FLAT at refetch 4.000 / held 2,304 with PEs pinned at
      144 across x0.125, x0.2019, x0.5, x0.8077, x1, x1.6154 -- a 13x clean range, zero

2026-09-09  SWEEP COMPLETE -- 196 EDP MAPPINGS, FIVE DESIGNS, NO CLEAN CAPACITY WIN
❌    eyeriss_v2_like_wglb IS THE DECISIVE NEGATIVE and it was my predicted WINNER. Its
      weight_noc holds 65,536 = 88.9% of the layer, sits ABOVE the PE array where it can
      absorb a DRAM-level loop, and has NO factors: pin at all. Over a 26x capacity range
      (x0.25 - x6.4615) with PEs pinned 192/192: refetch 7.000 at EVERY scale, held 3,072
      at EVERY scale. At x6.4615 the weight GLB has 423,936 weights of room and the mapper
      puts 1,536 in it -- 0.4% FILL. The refetch is `for P in [0:7)` at DRAM and the
      mapper prefers re-reading weights 7x to paying the activation/psum cost
❌    ECC_WEIGHT_FACTOR_RELAX does not rescue it either: every dilation pair still flat,
      held identical in both arms at fills from 0.8% to 57%
✅    BUT THE RELAX LEVER IS THE BIGGEST RESULT IN THE SWEEP, for a different reason.
      eyeriss_like layer3.0.conv1, SAME declared capacity, SAME 168/168 PEs:
        declared dataflow   refetch 14.000  DRAM w rd 4,128,768  417.17 uJ
        M=1,S=1 dropped     refetch  2.000  DRAM w rd   589,824  266.60 uJ
      A 7x CUT IN DRAM WEIGHT TRAFFIC AND 36% OF TOTAL LAYER ENERGY from relaxing ONE
      dataflow constraint -- where 61.9% more capacity buys 0%. (layer2.0.conv1 also goes
      8.000 -> 2.000 but PEs fall 168 -> 96 there, so only layer3.0.conv1 is clean.)
      NOT a reconstruction result and NOT the published chip -- it is a statement about
      where v1's DRAM weight traffic actually comes from, and it is not the buffer size
❌→✅ A FOURTH FALSE POSITIVE, caught by the guard's OWN test. capacity_verdict() was
      documented as "reads fell AND held ROSE" but the branch only excluded held-IDENTICAL,
      so a pair whose reads fell while held FELL still returned `capacity`. Hid because the
      real-cache property test swept only scope=exclusive; the shared caches had FOUR
      (e.g. WS x1 -> x1.6154: reads 147,456 -> 73,728 while held FELL 18,816 -> 9,984).
      Fixed: capacity now requires held to rise strictly; new ORDER? verdict for
      reads-fell/held-fell. The test now sweeps BOTH scopes and 12 refs = 37 REAL cached
      pairs, and reverting the fix fails by name on that exact row
❌    WS under scope=shared is SUGGESTIVE, NOT ESTABLISHED. It is the only place `capacity`
      verdicts survive (2 of 8 pairs) but the REFERENCE IS NOT CONVERGED: its refetch over
      monotonically rising capacity runs 4.000 1.000 4.000 1.000 2.000 2.000 2.000 1.000
      -- 1.000 at x0.5 then 4.000 at x0.75 with MORE room. The surviving +6.52% at x1.2115
      is measured against that x0.75 anomaly. Across all 8 pairs the recon arm hit 1.000
      at 7 of 8 capacities vs the embedded arm's 3 of 8 -- worth chasing, inseparable from
      search variance until the reference converges. 12 seed-variance jobs queued
      (41577788-99, seeds 12345/67890 at x{0.5,0.75,0.875,1.0,1.125,1.2115})
❌❌  THE SEARCH IS NOT CONVERGED, AND THE GAP IS BIGGER THAN EVERY ECC EFFECT IN THE STUDY.
      The capacity sweep is an excellent convergence probe: the SAME layer mapped many
      times at architectures differing only in a buffer depth, so any solution found at
      any capacity was reachable at all of them. eyeriss_like layer3.0.conv1, all EDP:
        PEs=84  cluster (x0.2019..x0.8077): refetch 2.000,  168-181 uJ, EDP 116-124
        PEs=168 cluster (x0.0505..x1.0):    refetch 14.000, 414-476 uJ, EDP 142-164
      x0.125 and x0.25 have IDENTICAL room (9,408) and differ 7x in refetch -- the split
      is PE count, not capacity. Within each cluster refetch is CONSTANT over 40x of room.
      AT THE DECLARED CAPACITY THE MAPPER RETURNED A MAPPING 24% WORSE IN ITS OWN
      OBJECTIVE (EDP 143.5) THAN ONE IT FOUND ON THE SAME LAYER AT x0.5 (EDP 115.9).
      Every ECC saving this study reports is 2-12%. The noise floor of the instrument is
      larger than the signal. This is FINDINGS section 2 with a number on it, and it bears
      on Tasks 1/2/3 as much as Task 4 -- those are differences taken WITHIN one cached
      mapping so they are internally consistent, but the mapping is not the optimum
✅    MONOTONICITY AUDIT over all 838 PE-matched pairs in 19 (design, layer, scope) cells:
      52 of 838 (6%) have a LARGER buffer refetching MORE. Under a capacity mechanism that
      must be 0%. The violations CONCENTRATE in exactly the 3 cells that produced all 4
      `capacity` verdicts: WS layer2.0.conv1 exclusive 45%, v2 layer2.0.conv2 22%, WS
      layer2.0.conv1 shared 12%. The other 14 cells are 0% and uniformly flat --
      eyeriss_v2_like_wglb layer2.0.conv1 is refetch 7.000 min AND max over 120 pairs.
      ALL FOUR `capacity` VERDICTS COME FROM THE THREE NOISIEST CELLS AND NONE FROM THE
      FOURTEEN QUIET ONES. On v2 layer2.0.conv2 the `capacity` row is 28.000 -> 7.000, but
      a buffer FIVE TIMES SMALLER than the reference reaches 4.000 on the same layer --
      a bad reference being escaped, not a capacity effect being detected
✅    Tables exported: results/tables/dilation_utilisation_{exclusive,shared,wrelax}.csv
      (206 rows: scale, w/PE, PEs used, MACs used/declared, room, held, fill, DRAM weight
      reads, refetch, total pJ, cycles, fullest other buffer, verdict, fingerprint, levels)
❌→✅ MY SEED-VARIANCE EXPERIMENT WAS INVALID AND env.sh SAID SO. ECC_MAPPER_SEED is in the
      cache fingerprint but reaches NOTHING -- `grep seed eccenergy/timeloop.py` is empty,
      and env.sh states "timeloop-mapper v4 exposes NO random seed, so this documents
      intent only." All 3 seeds produced BYTE-IDENTICAL stats (md5 9066dafc4ca7). 12 jobs
      spent measuring nothing. Should have read the knob's comment before submitting
✅    BUT IT REFRAMES THE RESULT, AND IN A USEFUL WAY. The mapper is DETERMINISTIC: same
      arch + same mapper settings + same thread count -> the same mapping byte for byte.
      So the non-monotonicity is NOT run-to-run randomness. It is a deterministic but
      CHAOTIC sensitivity to the architecture -- change a buffer depth by a few entries
      and random_pruned lands somewhere else, REPRODUCIBLY. Therefore:
        - results are reproducible; re-running changes nothing
        - the 6% "bigger buffer refetches more" rate is REAL and REPEATABLE, not sampling
          noise, and cannot be averaged away with seeds
        - the only lever is a STRONGER SEARCH (raise ECC_VICTORY until the answer stops
          moving with capacity)
✅    Victory-convergence test queued instead: WS shared, layer2.0.conv1, scales
      {0.5, 0.75, 0.8077, 1.2115} at ECC_VICTORY {10000, 50000} vs the cached 2000
      (41582123-30). This is the experiment that can actually settle the -74% GLB result

2026-09-10  THE CONVERGENCE TEST REVERSES THE HEADLINE -- victory 2000 is not usable
❌    WS scope=shared layer2.0.conv1, victory 2000 -> 10000:
        x0.5    emb  rf 1.000 273.2uJ  ->  rf 2.000 251.0uJ
        x0.75   emb  rf 4.000 280.4uJ  ->  rf 8.000 254.2uJ
        x0.8077 rec  rf 1.000 264.1uJ  ->  rf 8.000 256.4uJ
        x1.2115 rec  rf 1.000 276.7uJ  ->  rf 2.000 261.5uJ
      At victory 2000 recon x0.8077 beat its x0.5 reference by 9.1 uJ -- the best result
      in the sweep and the one I recommended for a YAML change. At 10000 the SAME pair
      REVERSES: recon 5.4 uJ WORSE, 4x the refetch. Energy falls everywhere (a better
      search) but the ORDERING BETWEEN THE ARMS FLIPS
❌    THE -74% operand_glb SAVING IS WITHDRAWN. It was search non-convergence, not
      capacity. No victory-2000 number in section 7.8 may be quoted without re-running
✅    CONVERGENCE IS NOW A GATE, written into prompt_2.md: map the embedded arm at
      victory {2000, 10000, 50000, ...}; converged means the last two agree within a
      stated margin, and that margin MUST be smaller than the ECC effect being claimed;
      re-check at the smallest memory as well as the largest. 50000 half still running
      (41582123-30)
✅    prompt_1.md written: the DRAM -> on-chip transfer cost. Model is 512 pJ per 64-bit
      access = 8 pJ/bit (Accelergy LPDDR4 plug-in, nobody chose or cited it) and
      ECC_DRAM_IF_FRAC=0.40 is an uncited ASSUMPTION. Together they turn a 38.1% cut in
      bits on the wire into a 2.1% cut in layer energy. Recommends citing both, adding an
      evaluator-side ECC_DRAM_PJ_PER_BIT override, and publishing a 2-D sensitivity
✅    prompt_2.md written: the next session's sweep prompt, with the 8 things wrong with
      this sweep, the SCALE-BY-4 trick for exact quarter-bit datawidths (width x4,
      depth /4, EVERY arm's datawidth x4 -- verified to reproduce floor(W/q) exactly for
      all five codes), the requirement that Baseline/Embedded/Recon share one hardware
      YAML differing ONLY in datawidth, and the convergence gate
